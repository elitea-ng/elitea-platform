# Accepted removals of a merged pull request's lines — one per line,
# '<pull request> <path>  # reason'. The pull request number carries NO
# leading '#': the checker treats everything from a '#' as a comment, so
# '#634 …' is an empty line. A missing file is an empty allowlist. Add an
# entry only for a removal that was the POINT of a later change, never to
# silence a squash that lost a fix.
634 services/elitea-main/internal/api/production_router.go  # #712 (P2.P9 cutover) deleted the vendored DeepWiki SPA that #634 introduced; its router mount went with it on purpose
682 apps/elitea-web/package.json  # #765 reverted the @babel/plugin-transform-runtime 8.0.1 bump on purpose: it requires Babel 8 and npm ci stopped resolving
682 apps/elitea-web/package-lock.json  # same revert (#765): the lock entries the bump added go with it
667 libs/proto/pyproject.toml  # #778 reverted the protobuf 7.36.0 pin on purpose: the Python worker requires protobuf <7 and pip could not resolve the two together
679 services/elitea-deepwiki/pyproject.toml  # #799 restored the frozen engine closure on purpose: eight bumps inside it (#677-#679, #757-#761) left pip unable to resolve the engine extra; the pins move with a re-copy of the engine, and Dependabot now ignores both frozen packages
