# binary-allowlist.txt — deliberate exceptions to scripts/no-binaries-check.sh.
# One repo-relative path or glob per line. Keep small; every entry is reviewed.
#
# Format examples:
#   docs/assets/logo.png
#   apps/*/public/**/*.woff2
#
# Cross-language runtime conformance vectors. These are canonical protobuf wire
# fixtures checked byte-for-byte by the Go and Python implementations.
testdata/proto/runtime/v1/**/*.pb

# Wave-2 onboarding tour screenshots — legitimate UI asset images for interactive tours.
apps/elitea-web/src/assets/onboarding/tips/*.png
apps/elitea-web/src/assets/onboarding/welcome/chat-welcome.png

# Wave-2 maintenance splash entry (A15) — same image as the onboarding welcome
# screen above, duplicated locally since this Vite entry point can't import
# across the src/entries/ boundary; imported by MaintenancePage.tsx.
apps/elitea-web/src/entries/maintenance/assets/chat-welcome.png

# Playwright visual-regression baselines (Wave 3, issue #61).
#
# These are references, not build artifacts: `toHaveScreenshot` compares against
# them, so a repo without them has a visual suite that cannot fail. They are
# regenerated only inside the pinned mcr.microsoft.com/playwright:v1.62.0-noble
# image, never on a developer's host.
#
# Deliberately narrow. It matches only the `-snapshots/` directories Playwright
# creates under e2e/snapshots/visual/, so it cannot quietly admit a stray binary
# elsewhere in e2e/. Total weight is separately capped: apps/elitea-web/scripts/
# check-visual-coverage.mjs fails the build if e2e/snapshots exceeds 12MB, which
# is the guard that actually matters here, since PNGs do not delta-compress and
# every revision stores a full copy in history.
#
# Caveat worth knowing before adding entries here: is_allowlisted() matches with
# bash's [[ == ]], where `*` crosses `/` — unlike filename globbing. So this also
# admits anything nested deeper under a `-snapshots/` directory. That is
# tolerable because Playwright writes baselines flat, but it means a pattern here
# is broader than it reads, and a `*` early in a path can admit far more than
# intended. Verified by matching this pattern against the real baseline path plus
# adversarial ones (e2e/mutant.png, e2e/snapshots/evil.png, src/assets/sneaky.png,
# dist/bundle.png) — only the baseline matched.
apps/elitea-web/e2e/snapshots/visual/*-snapshots/*.png

# Entity list-page empty-state illustrations (three pairs, one per family).
#
# Product art, not a build artifact: `src/shared/ui/EntityCardList/
# EntityEmptyState.tsx` imports all six and paints one pair per list page, the
# light/dark pick made in CSS. The baseline `apps/elitea-ui` ships the same
# three pairs as PNGs under `src/assets/images/` at 1446px and ~1.1MB each, so
# the repository rule here is "reviewed raster assets", not "SVG only" — the
# onboarding entries above are the same class. These are re-encoded to 720px
# WebP (~100KB each, ~600KB for all six) because the app never paints them
# wider than 240px.
#
# Same `[[ == ]]` caveat as the entry above: `*` crosses `/`, so this pattern
# also admits a .webp nested deeper under empty-states/. The directory is flat
# and holds only these six files.
apps/elitea-web/src/assets/empty-states/*.webp
