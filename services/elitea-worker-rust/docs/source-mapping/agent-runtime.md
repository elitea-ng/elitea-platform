# Agent runtime source mapping

This ledger covers both `agent.execute.application.v1` and
`agent.execute.adhoc.v1`. The language-neutral protocol remains Elitea-owned;
ADK-Rust is an internal execution substrate.

The reconstruction baseline for SDK behavior is the Python worker's production
lock: `elitea-sdk==0.9.8` at source revision
`b5113a129329b85d23c2d5c2bf55f18e307414ec`. The legacy Centry comparison is
`pylon_indexer/plugins/indexer_worker` at
`add5821eddf28f0aa9d7ba5757a53d3c25f9d4c1`. Standalone repository heads may
move independently, so they are drift evidence rather than an implicit change
to these pinned business contracts.

The platform Python worker is the executable contract reference for protobuf
input/output, control and output gRPC, `NodeEventV1` bridging, terminal result
binding, delivery fencing and replay. Rust parity therefore compares behavior
with both the schema and this Python implementation, not only with generated
message fields.

This is deliberately the runtime-first ledger. Direct saved agents and ad-hoc
agents, native toolsets, nested applications, MCP, durable HITL, checkpoint
resume and observability are completed before indexing/RAG. `adk-rag`,
pgvector metadata and index ingestion remain in `indexing.md`; they do not own
conversation state or block the direct-agent runtime checkpoint.

## Protocol and delivery

| Source path and symbol | Observable behavior | Rust target | ADK-Rust role | Proving tests | Status / deviation |
| --- | --- | --- | --- | --- | --- |
| Python worker `config.py::{RuntimeDeployConfig,RuntimeLimits,load_deploy_config,read_regular_file,validate_private_directory}`, `security.py::{RuntimeTrustMaterial,ExactEd25519PublicKeyResolver,_load_redis_password,load_ed25519_keyring}` and `serve.py::{serve_from_config,_serve_deployment_inner}` | Strict deployment config/trust, private-plane clients, shared `agentstate`, reconnectable Redis and one owned application/ad-hoc delivery runtime | `src/{lib,main,config,security,bootstrap}.rs`, `src/transport/redis_connector.rs`, `src/execution/production.rs` | None; this is Elitea process/delivery ownership below the ADK Runner | Config/file/keyring, Redis projection/error taxonomy, explicit/missing/malformed policy snapshot, CLI and data-free production-build tests; live certificate/Redis/PostgreSQL/process proof remains | Partial capability-disabled executable composition: concrete control/output/content/runtime-context/model channels, bounded shared PostgreSQL pool, per-generation credential-reloading Redis connector and the exact output/preparation/retirement/native delivery processor are wired. Rust `serve` leaves the shared deployment-v1 contract unchanged and requires a second bounded regular-file projection of the runtime `toolkit_security` dictionary. Missing and misspelled policy values fail closed; an explicit empty dictionary is valid. A multi-thread process owner installs SIGINT/SIGTERM first, emits stable data-free lifecycle/error fields and bounds the complete drain. Orchestrator/container policy projection, atomic refresh, capability registration and live cross-process proof remain closed |
| `libs/proto/elitea/runtime/v1/{agent,command,envelope,input}.proto` | Reference-only signed command and bounded application/ad-hoc input | `build.rs`, `src/protocol/{mod,command,wire}.rs`, `src/agents/protocol.rs` | None | `tests/{agent_command_contract,agent_input_contract}.rs`, Python-generated fixtures and mutation/limit corpora | Partial: signed agent command plus strict agent input are implemented; full offline envelope and other capabilities are planned |
| `libs/proto/elitea/runtime/v1/{control,output}.proto` | Claim, begin, authorize, lease, desired state, settlement, ordered output stream | `src/protocol/{control,output}.rs`, `src/execution/{agent_lease,agent_delivery_processor}.rs`, `src/transport/{control_grpc,output_session,output_grpc}.rs` | None | Python progress/success/cancellation/control goldens; claim/manifest/recovery mutation, control sum-type, supervised lease, message-boundary, ACK/credit/replay and whole-delivery corpora | Partial: the complete claim/recovery disposition sum type, begin/authorize decisions, non-clone submission/output authority, supervised lease/desired-state semantics, fresh and recovered settlement receipt validation, one-attempt control/output transports, the restricted Redis retirement boundary and the concrete fresh-delivery-to-native coordinator are implemented; deployed bootstrap and running/ambiguous output recovery remain planned |
| Python worker `protocol/codec.py::{parse_and_verify_signed_command,_scan_worker_command,_validate_command,Ed25519CommandAuthenticator}` | Verify exact signed bytes before decode; reject duplicate/unknown tags and capability mismatch | `src/protocol/{command,wire}.rs` | None | `tests/agent_command_contract.rs`: Python HMAC/Ed25519 fixtures, pre-auth tamper and authenticated wire/semantic mutation corpus | Partial: both agent entrypoints are admitted; other capability commands and file-backed offline envelope remain planned |
| Python worker `protocol/agent.py::{parse_agent_execution_input,request_from,bind_result_artifact}` | Canonical input parsing, semantic validation, exact content binding, terminal artifact binding | `src/agents/protocol.rs`, `src/agents/result.rs` | None | `tests/agent_input_contract.rs`: Python application/ad-hoc fixtures, duplicate/depth/string/number/truncation corpus and three supported terminal states | Partial: implemented through typed request and terminal artifact; delivery invocation remains gated |
| Python worker `handlers/agent.py::{AgentExecutionKind,AgentExecutionPayload,AgentExecutionHandler.execute}` and SDK `runtime/clients/client.py::{application,predict_agent}` | Select exactly one configured-application or ad-hoc entry point, then choose the saved application's direct-agent or pipeline runtime | `src/agents/{request,native_runtime}.rs` | One strict assembler over direct `LlmAgent` and stored `GraphAgent`; construction remains after authorization | Typed fixtures for application, ad-hoc, SDK-compatible missing `agent_type`, pipeline, malformed/unknown kind, exact-one-branch dispatch and completion-owner delegation | Capability-disabled native routing is implemented. Ad-hoc always selects the direct assembler; application `agent_type=agent` (including the current missing-field default) selects direct; `agent_type=pipeline` selects graph. Unknown/malformed kinds fail before either branch can consume runtime/session/model/tool/checkpoint authority. Both branches retain the same authorized lifecycle, projector, terminal selector and settlement owner. Production handler/bootstrap registration remains closed |
| Python worker `protocol/codec.py::build_node_event_output_frame` and Main `domain/runtime/fence.go::Fence.Validate` | Bind validated event to verified command identity, post-claim nonzero fence, sequence, digest and 64 KiB frame | `src/protocol/output.rs` | None | `tests/node_event_contract.rs::exact_python_vectors_match_browser_proto_and_claim_bound_output_frame` and identity/fence mutations | Implemented for nonterminal agent progress; Rust rejects an all-zero fence token before Main, closing a legacy Python-helper gap |
| Python worker `protocol/codec.py::{build_output_frame,_runtime_error_message}` and Main `application/output/agent_execution.go` | Bind agent result or registered failure to terminal identity, exact payload digest and deterministic settlement proposal | `src/protocol/output.rs`, strengthened result checks in `src/agents/result.rs` | None | `tests/agent_output_contract.rs`: exact Python success/cancellation frames, all nine safe error mappings, digest/settlement and malformed-result corpus | Implemented terminal frame construction, terminal ACK proof and typed PrepareSettlement request/receipt. Rust rejects zero digests and artifacts over 64 KiB before Main; Redis ACK ordering remains planned |
| Python worker `transport/output_spool.py::EncryptedOutputSpool` | Bounded AES-256-GCM file format, idempotent immutable put, sorted replay, exact atomic replacement and ACK deletion | `src/spool.rs::{EncryptedOutputSpool,SpooledFrame,SpoolLimits}` | None | `tests/output_spool_contract.rs`: encryption, capacity, replacement, replay, corruption, unsafe entries, permissions, ownership and cleanup; Python fixed-nonce golden | Implemented synchronous primitive for macOS/Linux. Rust uses directory-relative filesystem calls, validates owner-private files and holds an exclusive advisory child lock |
| Python worker `serve.py::{_execution_spool_binding,_prepare_execution_spool}` | Seven-field length-prefixed identity, SHA-256 child path, HKDF-SHA256 execution key and stream AAD | `src/spool.rs::{ExecutionSpoolIdentity,ExecutionSpoolBinding,SpoolMasterKey}` | None | `src/spool.rs::tests::binding_hkdf_and_fixed_nonce_ciphertext_match_python` generated by `tests/fixtures/generate_output_spool_fixtures.py` | Implemented without caller-supplied derived key/path/AAD, preventing identity drift |
| Python worker `transport/output_grpc.py::OutputGrpcSession::{start,send,_restore_pending,_replay_pending}` | Validate one bootstrap grant, restore exact deterministic frames, block new admission behind replay, persist before send and await one bound ACK per replayed frame | `src/transport/output_grpc.rs::OutputGrpcSession`, `src/transport/output_session.rs::{OutputSessionState,DurableOutputFrame}` | None | Unit state corpus plus encrypted-spool component cases `lost_ack_preserves_exact_spool_for_next_session_replay` and `early_eof_during_replay_preserves_the_durable_frame` | Implemented for one attempt. Delivery-owned bounded fresh-session reconnect is planned rather than hidden inside the stream |
| Python worker `transport/output_grpc.py::OutputGrpcSession::_accept_ack` and Main `runtimegrpc/output/server.go::Server.Publish` | Absolute credit replacement, exact stream/identity/fence/watermark binding, monotonic committed prefix and typed cancellation/deadline/retry results | `src/transport/output_session.rs::{validate_ack,commit_ack}` | None | Bootstrap-once, credit ceiling, forged fence, backward/beyond-write ACK, draining and winner mutation tests | Implemented. Rust additionally bounds durable counters to PostgreSQL `BIGINT`; protobuf ACK duplicate/unknown-field inspection is limited by tonic/prost and recorded below |
| Python worker `transport/output_grpc.py::OutputGrpcSession::{replays,replace_pending_exact,replace_pending_cancelled_recovery,replace_pending_ambiguous_recovery}` and `execution/delivery.py::_reconnectable_output` recovery decisions | Inspect durable bytes before networking; exact-CAS the sole pending frame; allow only advancing fresh-fence cancellation/ambiguous recovery; retire only a completely covered prefix | `src/transport/output_grpc.rs::PreparedOutputSpool` | None | No-network prepare/reopen, post-terminal rejection, complete-prefix reconciliation, exact terminal CAS and cancelled/ambiguous rebind mutation tests | Implemented as an explicit pre-network typestate boundary and consumed by the fresh-agent preflight. A reconciled object cannot be reused, and stream connection consumes the prepared spool; bounded reconnect and exact pending-frame recovery remain delivery-coordinator work |
| Python worker `execution/delivery.py::_process_admitted` output-spool inspection before `_reserve_execution_capacity` and `_begin_execution`, including module function `_validate_pending_output` | Open and exclusively own the execution-bound encrypted spool before capacity, Begin, input or authorization; route exact terminal output to replay, leave exact progress as recovery-required/no-ACK, reconcile only stale progress covered by the authenticated claim handoff watermark, and fail closed for every other pending state | `src/execution/output_delivery.rs::{AgentOutputPreflight,EmptyAgentOutput,AcceptedTerminalOutputRecovery,AgentOutputRecoveryRequiredNoAck,AgentOutputSpoolReopener}`, `src/protocol/{output.rs::validate_restored_agent_output_frame,control.rs::AcceptedTerminalClaimRecovery}`, plus `src/spool.rs` and `src/transport/output_grpc.rs::PreparedOutputSpool` | None | `src/execution/output_delivery_tests.rs`: empty exclusive ownership, producer/workload substitution before directory creation, exact progress/terminal classification, covered stale-progress reconciliation, foreign-fence rejection, terminal payload-digest mutation, admitted-request substitution and changed-terminal rejection across reopen; every preparation test constructs the required empty-spool proof | Implemented no-network admission and classification boundary. Preflight runs filesystem/crypto work on Tokio's blocking pool, binds both output metadata identities to the sealed accepted claim and revalidates the decrypted frame's signed-command identity, deterministic event IDs, canonical NodeEvent/runtime result, payload digest, settlement proposal and admitted request version/content digest. `prepare_fresh_agent_invocation` accepts only `EmptyAgentOutput`, so pending output cannot reach reserve or Begin. Terminal classification consumes and destroys the accepted input manifest, retaining only an opaque lease/output recovery binding plus a sealed execution-bound spool reopener which requires the same deterministic terminal bytes on every later attempt; no network, settlement or Redis effect is claimed here |
| Python worker `transport/output_grpc.py::secure_output_channel`, `_validated_metadata` | mTLS output endpoint, 64 KiB request/80 KiB response limits and allowlisted session metadata | `src/transport/output_grpc.rs::TonicOutputStream` plus planned deployment TLS composition | None | Configuration/message-limit/metadata unit tests; real mTLS component test planned | Partial: tonic sets exact encoding/decoding ceilings and sends each metadata key once over an injected channel. CA/certificate/key loading and hostname policy are not yet implemented |
| Python worker `transport/control_grpc.py::{ExecutionControlClient,_validated_metadata}` | One deadline-bound RPC attempt, 64 KiB request/80 KiB response bounds and exact workload metadata | `src/transport/control_grpc.rs::{ControlGrpcClient,TonicControlRpc}` | None | Injected component transport covers all six methods, exact request/response limits, metadata/deadline mutations and data-free one-attempt failure | Implemented transport only. Rust requires ASCII because these are non-binary gRPC metadata values; production Python also supplies ASCII deployment identities. Semantic response validation is owned by `src/protocol/control.rs`; TLS file/hostname policy remains a deployment gate |
| Python worker `transport/input_content.py::{ClaimBoundInputRequestBuilder,ScopedInputContentClient.fetch_materialized}`, Main `infra/storage/content_server.go::ContentServer` | Redeem one post-claim input over HTTPS/HTTP2 using the exact claim/fence headers and escaped route; bind the derivative body to its admitted source version, length and SHA-256 | `src/protocol/control.rs::{LeaseMonitoredAgentExecution,ClaimBoundInputAuthority}`, `src/transport/input_content.rs::{MaterializedInput,InputContentClient}` | None; borrowed materialized bytes feed `src/agents/protocol.rs` before ADK assembly | `src/transport/input_content_tests.rs`: exact route/header vector, pre-network source ceiling, source and response digest/length/version mutations, HTTP/status/cache/media/duplicate-header failures, exact configured body limit, timeout and transport classification | Partial: the one-attempt adapter is implemented with a 1 MiB protocol ceiling and a configurable deployment ceiling (current Python/Main production is commonly 256 KiB). Public redemption requires the opaque state minted only after `BeginExecution` and the mandatory supervised Renew -> Observe poll; raw claim/fence construction and injected transports are test-only. The production constructor requires a private CA plus worker client identity and builds its own tonic endpoint from the validated HTTPS origin; hostname verification is derived from that origin and channel-origin substitution is impossible. Rust additionally requires HTTP/2 and `Content-Length`, rejects trailers and noncanonical base64, keeps accepted bytes in a non-clone/non-debug zeroize-on-drop wrapper, zeroizes partially collected rejected bodies, and exposes stable error codes/retry classification while retaining transport error sources. Intentional error improvement: Python treats every non-401/403 non-2xx response as retryable dependency failure; Rust follows Main's concrete meanings and classifies 400/404/413/422 as nonretryable invalid/resource failures while retaining 5xx as retryable. Real Main-to-Rust mTLS/HTTP2 component proof and full delivery-coordinator wiring remain gates |
| Python worker `serve.py::_serve_deployment_inner`, `agents/client_context.py::{RuntimeExecutionClaim,EliteaClientContext,ClaimBoundEliteaClientContextFactory}`, `agents/sdk_adapter.py::EliteaSdkAgentAdapter.from_context` and `transport/runtime_context.py::ClaimBoundEliteaTokenClient`; Main `infra/storage/{content_server.go::PostEliteaClientToken,index_runtime_context.go::EliteaClientTokenService.Resolve,postgres_index_runtime_context.go::AuthorizeRuntimeContext}`, `transport/runtimegrpc/control/server.go::Server.AuthorizeInvocation`, `application/execution/claims.go::ClaimService.AuthorizeInvocation` and `infra/db/repos/claims.go::ClaimsRepository.AuthorizeInvocation` | Redeem the execution actor PAT from the exact live claim/fence/resource project over a dedicated non-caching mTLS HTTP/2 request; never cache, persist or log the credential | `src/protocol/control.rs::ClaimBoundRuntimeContextAuthority`, `src/agents/runtime.rs::{AuthorizedNativeAssembly,AdmittedOrdinaryNativeAssembly,RedeemedOrdinaryNativeAssembly}`, `src/transport/runtime_context.rs::{RuntimeContextClient,ClaimScopedEliteaContext}` | None; the later model/tool clients may borrow the zeroized context only after assembly admission | `src/transport/runtime_context_tests.rs`: exact escaped route/empty POST/header vector, HTTPS origin normalization, HTTP2, redirect/401/503 classification, cache metadata, content-length/body ceilings and mismatch, duplicate/extra JSON fields, schema/project/control-character mutations, timeout/transport typing and redaction; `src/agents/assembly_tests.rs::credential_redemption_is_unreachable_until_the_profile_is_admitted` | Implemented as a capability-disabled post-`AUTHORIZED_NOW` Rust authority boundary. Only the authenticated `AUTHORIZED_NOW` branch duplicates the 32-byte fence into a zeroize-on-drop, non-clone/non-debug grant; ALREADY/rejected/unknown paths cannot mint it. The grant stays inseparable from the request/output/lease/submission/Redis owner, is consumed by the first assembly attempt, and becomes redeemable exactly once only after strict profile admission. Main's redemption endpoint validates the live claim/fence/project but does not currently enforce invocation state, so the safer ordering is worker-local rather than misrepresented as a server predicate. The transport builds its own credential-redemption-origin-bound mTLS channel, requires HTTP/2 and no-cache metadata, bounds the exact JSON body to 32 KiB and the PAT to Main's 16 KiB producer limit, and returns a non-clone/non-debug zeroized context. It deliberately does not derive the later SDK/model destination from that content-service origin; provider assembly must bind a separately validated platform origin. Intentional improvement over Python: PAT issuance moves from pre-authorization input resolution to the cancellation-safe post-authorization assembly phase, so credentials do not exist before the final invocation fence. Wrong or duplicate response headers, trailers, interrupted bodies and exact PAT boundary mutations remain explicit transport-hardening test gaps alongside real Main TLS proof and provider/tool consumption |
| Main `agentexecution/{start.go::currentApplicationInput,adhoc.go::currentAdhocInput,continue.go::ContinueCurrentAgent,regenerate.go::RegenerateCurrentAgent,tools.go::resolveCurrentAgentModel}` and `internal/db/queries/agent_chat.sql::{ResolveCurrentApplicationTurn,ResolveCurrentAdhocTurn,ResolveCurrentRegeneration,ResumeCurrentAgentHITL}`; Python worker `agents/sdk_adapter.py::{execute_application,execute_adhoc,_invoke_initial_agent,_discard_regenerated_thread}`; legacy Centry `methods/{indexer_agent,indexer_predict_agent}.py`; pinned SDK `runtime/clients/client.py::{application,predict_agent,get_llm}` | Preserve direct saved-agent/ad-hoc selection, frozen model controls, text history, exact response regeneration and configured/nested/MCP tool participation; admit one exact read-only sensitive-tool continuation and fail closed for other continuation, skill, attachment and context surfaces | `src/agents/{native_runtime,assembly,ordinary,session,application_tools,context_management,sensitive_tools,direct_hitl}.rs`, `src/toolkits/{snapshot,materialize,mcp}.rs`, `src/agents/runtime.rs` | Direct ADK `LlmAgent`/Runner with native `Toolset` and `AgentTool`; Elitea owns claim, policy, frozen identity, event projection, durable continuation and exact-session regeneration | Assembly/session/provider tests plus strict native-router, ordinary application/ad-hoc, regeneration reset/no-old-assistant, configured-tool, MCP, nested-child and exact sensitive-MCP pause/resume E2E loops; Private-project UI regeneration/continuation/reload proof | Partial capability-disabled runtime. Both entrypoints use one direct `LlmAgent`, exact frozen model project, bounded text history and native toolsets; saved direct children resolve once per assembly and run as `AgentTool`, and saved fixed-origin HTTP MCP toolsets use ADK's client. Direct/ad-hoc `is_regenerate` is admitted separately from fresh and resume: the claim-fenced exact session is deleted and recreated, seeded from Main's truncated history, and runs the original question once without the replaced assistant branch. Sensitive tool calls pause through native confirmation. A second claimed request can restore one read-only call by exact `interrupt_id` without provider replanning; invocation-local storage and effectful calls fail before execution. Frozen applications now enter one strict native router which delegates direct agents or graph-owned pipelines without changing the surrounding authorized lifecycle. Saved-pipeline regeneration, internal/lazy tools, variables/templates, rich content, skills, attachments, MCP OAuth continuation, replay-window restart recovery, effect receipts and context editing, a dedicated summary model and pipeline-graph context management remain closed |
| Production-pinned SDK `runtime/clients/client.py::EliteAClient.get_llm`, `tests/runtime/test_5113_anthropic_prompt_caching.py`, and LangChain request normalization; Main public model proxy `/llm/v1/{chat/completions,messages}` | Route `openai_compatible=true` through the existing OpenAI-compatible contract and native Anthropic through `/messages`, using the frozen model's project authority | `src/transport/model_facade.rs::{ModelFacade,BoundModelFacade}`, `src/transport/openai_compatible_facade.rs::{ModelGatewayClient,EliteaOpenAiCompatibleModel}`, `src/transport/anthropic_facade.rs::EliteaAnthropicModel`, `src/agents/ordinary.rs`; gateway `internal/{llmproxy/models.go,account/account.go,account/credential_selector.go}` | ADK provider-neutral `Llm`/request/response types plus `AnthropicSchemaAdapter`; Elitea wraps origin/TLS, actor token, model-project identity, bounded stream and completion proof | Exact compatible/native wire and stream corpora; root/child E2E asserts model projects 17/23 independently of the execution actor project; isolated Sonnet-through-compatible real-provider proof | Foundation capability-disabled with one real OpenAI-compatible proof. The facade selects explicit OpenAI-compatible or Anthropic adapters over one origin-bound mTLS pool. Rust sends frozen `model_project_id` as `OpenAI-Organization`, allowing admitted shared/project-bound models without weakening actor-token authorization. Execution `868af70730dc53444aeb27b7343f0f51` streamed and persisted `eu.anthropic.claude-sonnet-4-6` through the OpenAI-compatible adapter, Main, Bifrost and a local compatible proxy. A normal `open_ai` credential with a custom base maps to Bifrost's per-key compatible provider; an official OpenAI base stays native. The gateway accepts `/llm` or `/llm/v1` by removing the version suffix before Bifrost adds it once. Native OpenAI, native Anthropic, tool-call provider proofs, native citations, context editing, a dedicated summary model and pipeline-graph context management and bounded pre-stream retry remain gates |
| SDK `runtime/tools/llm.py::{_normalize_completion_status,_continue_nested_output,_merge_continuation_text}` and direct-chat output-limit policy; Main `agentexecution/continue.go`, `agent_chat.sql::{ResolveCurrentAdhocTurn,ResolveCurrentOutputLimitContinuation,ResumeCurrentAgentOutputLimit}`; EliteaUI output-limit normalization and Continue handler | Preserve provider Auto semantics, persist a bounded partial root response, resume the exact response/session once without repeating its prefix, and keep later turns independent | Main current model snapshot and output-limit admission; Rust `src/agents/{assembly,session,events}.rs` and `src/transport/{openai_compatible_facade,anthropic_facade}.rs`; UI `agent.continue.output-limit.v1` route | Native `LlmAgent` plus claim-fenced `SessionService`; Elitea owns exact response identity, one-use continuation admission, partial-text projection and seam merge | Model Auto/request-wire tests, ad-hoc participant-settings PostgreSQL regression, overlap/separator stream-and-terminal tests, and local Private-project UI proof with 700-token pause, reload, 200 resume, exact markers, durable completion and fresh follow-up | Partial capability-disabled. OpenAI-compatible Auto omits the output limit, while native Anthropic receives the frozen configured maximum required by its API. A root length stop records one sequence and visible partial text; reload reconstructs the Continue card, and Main consumes that sequence while retaining conversation/message/question/thread/generation identity. Ad-hoc resume reads `llm_settings` from the selected dummy/model mapping, not the user mapping. Rust restores the same ADK session, removes bounded meaningful overlap and repairs a missing alphabetic separator before both streaming and terminal projection. Current root continuation is user-triggered, matching the SDK direct-chat branch. Project-controlled automatic root continuation, the SDK's four-round nested/pipeline policy, and graph suffix crash recovery remain separate gates |
| Python worker `transport/redis_commands.py::RedisCommandConsumer`, `transport/redis_asyncio.py::RedisAsyncioControlClient`, `serve.py::WorkerServeLoop`, and Main `transport/redisdispatch/{producer,stream_capacity}.go` | Consume one existing group with bounded `XREADGROUP`, fairly reclaim and heartbeat queued/active PEL entries, preserve exact signed bytes, stop intake then drain without business cancellation, and permit only atomic owner-bound `XACK + XDEL + delivery-index HDEL` after durable terminal authority | `src/transport/redis_commands.rs::{RedisCommandDelivery,RedisCommandRetirer,RedisRetirementClient}`, `src/transport/redis_streams.rs::{RedisStreamsClient,RedisStreamsConfig,RedisTlsMaterial,RedisReclaimPage}`, `src/transport/redis_generation.rs::RedisStreamsHandle`, `src/execution/{redis_delivery,agent_delivery_processor}.rs` | None; Redis delivery remains outside ADK execution/session state | Transport/generation tests cover canonical URL/limits, binary/duplicate/response-count corpus, Redis 7 deleted-ID reclaim shape, retirement tuple, cancellation ownership, replacement races and secret redaction; runtime tests prove fair scheduling, queue-plus-active and worker bounds, duplicate suppression, processing-lifetime heartbeat through drain, cancellation containment, delayed replacement and drain timeout; whole-agent processor tests cover both entrypoints through retirement and stopped admission; `tests/redis_command_retirement_contract.rs` covers exact-envelope and authority substitution | Implemented restricted transport, capability-disabled structured runtime and concrete application/ad-hoc processor with redis-rs 1.4.1 over worker-owned TCP/Rustls streams. Separate blocking-intake/control connections, RESP2, exact private CA plus client identity, TLS 1.3 only, bounded queues/concurrency, one command attempt and no hidden reconnect policy remain enforced. Retryable failure invalidates only the exact generation; the runtime explicitly establishes one replacement after a bounded stop-aware delay. It reserves queue-plus-active capacity before Redis, alternates bounded new reads with monotonic reclaim, retains non-cloneable PEL ownership through processing, and keeps heartbeats active after Stop until existing workers drain. The processor awaits the complete route/preflight/prepare/supervise/retire lifecycle, so a PEL entry remains owned until it is retired or deliberately returned for recovery. Runtime cancellation or drain timeout aborts its owned task set and leaves uncertain entries pending; accepted native work remains process-supervised, so the later bootstrap must enforce one process-wide shutdown budget and close the coordinator after delivery drain. It exposes no group creation, `XADD`, general ACK/delete, arbitrary Lua or key/value API. Production TLS-material/config loading, process signals, input-free running/ambiguous output recovery, real TLS/ACL Redis 7 and hostile-server pre-allocation RESP proof remain gates; see `source-mapping/redis-command-delivery.md` |
| Python worker `execution/delivery.py::{_claim_receipt,_accepted_agent_claim,_validated_claim_entries,_validate_receipt_identity,_validate_active_fence}` and Main `runtimegrpc/control/server.go::{ClaimCommand,verifyResolvedManifest}` | Seal one fresh, exact command/fence/manifest authority only after authenticated claim | `src/protocol/control.rs::{AgentControlClient::claim_agent,AcceptedAgentClaim}` | None | `tests/agent_control_contract.rs`: deterministic Python vector plus identity/fence/digest/role/count/expiry mutation matrix | Implemented for fresh `ACCEPTED` agent claims. Raw response parsers are private, and no recovery disposition can be coerced into `AcceptedAgentClaim` |
| Python worker `execution/delivery.py::{_validate_obsolete_receipt,_validate_retired_receipt,_validate_recover_running_receipt,_terminal_ack_recovery,_prepared_settlement_recovery}` and Main `claims.go::ClaimDecision.validate` | Interpret all ten claim dispositions without leaking inputs into recovery, distinguish ACK/no-ACK authority, and never re-enter the SDK after ambiguous invocation | `src/protocol/control.rs::{AgentControlClient::claim_agent_delivery,AgentClaimDecision,AgentOutputRecovery,RecoverTerminalAck,RecoveredSettlement,TerminalCommandAck}` | None | Python-generated vector for every disposition; input/authority leakage, retirement, proposal digest and exact settlement replay mutation cases | Implemented closed recovery admission. Only `Accepted` contains business input; active/running/ambiguous states expose output recovery plus a unique lease handle, while terminal proposal/receipt types permit only settlement replay or Redis retirement. `SETTLED_ACK` remains consumer-supported but no current Main repository producer branch was found |
| Python worker `execution/delivery.py::{_begin_execution,_authorize_invocation}` and Main durable claim state | Distinguish restart-safe preparation from the one durable native-ADK submission fence; retain output authority for authenticated semantic failures while treating transport loss as unknown-effect no-ACK recovery | `src/protocol/control.rs::{BeginAgentExecution,LeaseStartingAgentExecution,LeaseMonitoredAgentExecution,InvocationAuthorizationCandidate,InvocationAuthorizationPayload,InvocationAuthorizationDecision,InvocationAuthorizationNoAckAuthority}`, `src/execution/agent_preparation.rs::{PreparedAgentAuthorizationPayload,AuthorizedAgentRun,AgentFailureTerminal,AgentAuthorizationUnknown}` | The later ADK entry consumes the inseparable submission/output/prepared-run state exactly once | Exact Python decision vectors, application/ad-hoc payload carry-through, all closed authorization outcomes, server-text redaction, mixed-response rejection, direct authorization-terminal publication and owned typestate transitions | Implemented semantic boundary. `STARTED_NOW` is not an invocation token; business input remains inaccessible until the immediate supervised lease poll activates it. Preparation atomically binds the request, verified command, Redis delivery, capacity reservation and supervised lease to its claim before the authorization call. Only `AUTHORIZED_NOW` converts that complete payload directly into one opaque authorized run. `ALREADY_AUTHORIZED`, authenticated rejection and malformed authenticated response consume the prepared payload and exact output claim together into one terminal-only state while dropping all request authority. Transport uncertainty drops request authority, retains only local lease/capacity/empty-spool cleanup plus opaque no-ACK claim ownership, and grants neither terminal nor submission authority. No tuple/map escape hatch can authorize claim A while publishing or submitting claim B |
| Python worker `execution/delivery.py::{_ClaimLeaseMonitor,_await_with_lease_state}` | Exact-fence deterministic renewal keys; sequential Renew -> Observe; post-observation wall-clock margin; monotonic skip cadence; sticky Stop while renewal continues; fatal-over-cancellation priority; cancellation of pre-SDK async work only | `src/execution/agent_lease.rs::{ClaimLeaseMonitor,ClaimLeaseActivation,ClaimLeaseError}`, `src/execution/native_agent_lifecycle.rs`, crate-private control operations over `src/protocol/control.rs::ClaimLeaseHandle` | None | `src/execution/agent_lease_tests.rs`: exact call order/key sequencing, recovery start without a pre-replay poll, 1ms/10s v1 cadence bounds, post-observation margin, cancellation latch, later fatal priority, DRAINING policy, missed-tick skip, pre-invocation cancellation and stalled-RPC close; `tests/agent_control_contract.rs` checks the exact Python renewal-key vector and mandatory activation typestate; native lifecycle tests cover Stop and late deadline | Implemented supervised lease lifecycle and native composition. The actor owns the only claim-and-handle package, uses bounded cancellation-safe command/state channels, and keeps renewing after user Stop while the invocation-exclusive Runner drains. Public configuration accepts only whole-millisecond intervals from 1ms through the deployed 10s maximum; only `ClaimLeaseActivation::Active` exposes business inputs. Exact accepted-terminal recovery consumes an input-free claim into the same actor but deliberately does not poll before the first durable replay. The fresh lifecycle retains the monitor through real ADK EOS, final Renew -> Observe, terminal ACK, settlement and Redis retirement. Main does not currently deduplicate renewal keys, so the generated key is correlation intent rather than claimed server replay protection |
| Python worker `execution/delivery.py::_process_admitted`, `_await_with_lease_state`, agent `_resolve_inputs` and `_execute_resolved` pre-authorization half | After empty-spool proof, reserve before Begin; require `STARTED_NOW`; complete immediate Renew -> Observe before input; race materialization with Stop/fatal lease; validate canonical application/ad-hoc request; sample inclusive deadline after full request construction; stop before `AuthorizeInvocation` | `src/execution/output_delivery.rs::{EmptyAgentOutput,PreparedAgentOutput}`, `src/execution/agent_preparation.rs::{prepare_fresh_agent_invocation,PreparedAgentInvocation,PreInvocationTerminal}` | None before authorization; the later native ADK runner consumes the prepared request and opaque reservation | `src/execution/{output_delivery_tests,agent_preparation_tests}.rs`: spool-first ownership plus shared application/ad-hoc ordering, saturation, Begin replay, immediate/periodic Stop, cancelled input future, input failure plus final lease poll, fatal-over-terminal, dependency mapping, inclusive and request-validation deadline crossings | Implemented fresh preparation boundary. `PreparedAgentOutput` keeps the locked empty spool and its exact execution-bound reopen factory inseparable through prepared and terminal-only states. Stop, input and deadline causes retain exact pre-invocation output authority for canonical terminal publication; lease/control/clock failures remain typed no-ACK errors. `PreparedAgentInvocation` keeps the exact delivery, verified command, live lease, validated request, capacity slot and output owner private, so callers cannot authorize or submit by copying raw fence data |
| Python worker `execution/delivery.py::_process_admitted`, `_publish_with_terminal_linearization`, `_publish_output` and `protocol/codec.py::{build_output_frame,_runtime_error_message}` pre-invocation failure path | For Stop, deadline, input or authenticated authorization failure, perform one final Renew -> Observe; fatal lease loss suppresses output; Stop beats inclusive deadline; sample occurrence time after the poll; publish sequence `handoff + 1`; durably ACK the terminal before settlement and Redis retirement | `src/execution/agent_preparation.rs::AgentFailureTerminal`, `src/protocol/control.rs::AgentExecutionOutputAuthority::bind_failure_terminal`, `src/execution/output_delivery.rs::{publish_pre_invocation_terminal,publish_agent_failure_terminal,PreparedAgentOutput::persist_terminal}`, `src/execution/{agent_coordinator,agent_invocation}.rs` | None; this path is strictly before native ADK submission | `src/execution/output_delivery_tests.rs`: final poll/order, byte-identical reconnect, exact handoff sequence/time, deadline override, Stop-over-deadline, fatal lease suppression, bounded exhaustion, dropped authorization wait, stopped-coordinator explicit cleanup, authorization-request destruction and capacity release | Internal coordinator implemented and capability-disabled. Preparation and authenticated authorization failures converge on the same sealed owner; authorization failure mapping is closed and the already-validated request is no longer retained or accessible before publication. The claim authority is consumed while binding the terminal, so the raw fence is never exposed; the sequence is derived rather than caller-selected. Persistence completes on the blocking pool before the first stream attempt. The shared bounded replay/replacement helper permits only exact frame-bound cancellation/deadline CAS, then enforces ACK -> PrepareSettlement -> exact-envelope Redis retirement. `AgentInvocationCoordinator::submit` constructs and synchronously transfers the authorization future plus exact reservation to its process supervisor before the RPC can start, so dropping a result waiter cannot discard an unknown effect or an authorized continuation. A stopped coordinator returns an explicit non-cloneable rejected owner whose cleanup releases the unstarted lease/grants and capacity without authorization. Coordinator futures are boxed at the ownership boundary to keep task allocations bounded |
| Python worker `execution/delivery.py::_IndexProgressOutput::{publish_from_sdk,_publish,prepare_terminal,_close_active}` and Main `runtimegrpc/output/server.go::Publish`, `NodeEventsRepository.ProjectNodeEvent`, `agent_execution_results.go::loadCurrentAgentTerminal` | Allocate one contiguous progress sequence at a time, bind it to the exact accepted fence/handoff, and advance only after Main's bound durable ACK; only an exact frame-bound cancellation/deadline winner may replace rejected progress at the same sequence; hash the exact ACKed canonical `full_message` browser JSON and publish the request-bound terminal at `N + 1` | `src/protocol/control.rs::{AgentExecutionOutputAuthority::try_into_output_cursor,AgentExecutionOutputCursor,ClaimBoundAgentProgress,ClaimBoundAgentTerminal}`, `src/execution/agent_preparation.rs::CursorBoundAuthorizedAgentRun`, `src/execution/output_delivery.rs::FreshAgentProgressPublisher`, `src/execution/native_agent_lifecycle.rs`, and `src/transport/output_grpc.rs::{OutputGrpcSession::send,PreparedOutputSpool::start_progress_replay,ProgressReplaySession,ProgressReplayDecision,DurablyAckedProgress,FrameBoundProgressRejection}` | ADK events remain semantic inputs; Elitea owns sequencing, encrypted persistence, retry and durable authority | Cursor mutation/substitution tests plus `src/execution/output_delivery_tests.rs::{fresh_progress_keeps_one_live_session_across_exact_acked_events,uncertain_live_progress_reopens_and_replays_the_exact_durable_frame,cancelled_live_wait_recovers_the_retained_ack_without_resending,cancelled_replay_wait_resumes_the_same_owned_replay_session,changed_durable_progress_is_rejected_before_a_replay_session_starts,frame_bound_main_rejection_is_retained_without_resending,acked_full_message_binds_the_exact_canonical_browser_bytes,generic_or_unacknowledged_progress_cannot_mint_a_completed_terminal,restored_full_message_ack_retains_the_same_terminal_artifact_authority,application_and_adhoc_share_native_events_terminal_settlement_and_redis_retirement,durable_stop_interrupts_only_the_owned_run_then_settles_cancelled,durable_stop_cancels_stalled_post_authorization_assembly_before_runner_start,deadline_during_native_run_preserves_completion_events_but_replaces_terminal_success}` and transport bootstrap/replay cancellation tests | Implemented capability-disabled claim-bound cursor, bounded fresh progress publisher and owned terminal composition. The owner retains the exact frame before every await, ACKs a whole projected batch before polling ADK again, reuses one live stream, and accepts only byte-identical restored progress. Only the specialized `full_message` path hashes canonical browser JSON before the first await, and only its exact bound ACK can create the admitted-request-bound completed result. The shared native lifecycle drains real ADK EOS, performs final Renew -> Observe, applies fatal lease > Stop > inclusive deadline > completion, publishes either terminal `N + 1` or an exact same-sequence Main-winner replacement, then enforces terminal ACK -> settlement -> exact-envelope Redis retirement. Production provider/session/tool and non-ordinary event assembly remain closed |
| Python worker `execution/delivery.py::{_prepare_frame_settlement,_settlement_receipt,_terminal_ack_recovery,_prepared_settlement_recovery}` | Only a verified-command-bound durable terminal ACK or exact state-owner recovery proposal can create PrepareSettlement; an already-prepared receipt skips the RPC | `src/transport/output_grpc.rs::{OutputGrpcSession::send_terminal,DurablyAckedTerminal}`, `src/protocol/control.rs::{AgentControlClient::{prepare_agent_settlement,prepare_recovered_agent_settlement},RecoverTerminalAck,RecoveredSettlement,SettlementReceipt,AgentCommandRetirementAuthority}` | None | Bound ACK to consumed settlement proof component test; Python fresh/recovery vectors, deterministic digest, command-identity mutation and no-second-RPC assertions | Implemented fresh and restart settlement authority. Raw success/recovery protobufs cannot construct proofs, the output session requires the original verified command before minting terminal authority, and every final proof retains the exact authenticated outer-envelope bytes, command identity and stable delivery ID for Redis retirement |
| Python worker `execution/delivery.py::{_recover_local_output,_publish_with_terminal_linearization,_publish_output,_reconnectable_output}` | Start lease supervision without Begin/input/ADK, attempt the exact terminal before a new desired-state poll, retry only reconnectable failures with a fresh byte-identical spool owner, and permit one exact-CAS cancellation/deadline replacement with the original sequence and occurrence time | `src/execution/output_delivery.rs::{recover_accepted_terminal,AgentTerminalReplay,AgentTerminalRecoveryConfig,AgentOutputSpoolReopener}`, `src/execution/agent_lease.rs::ClaimLeaseMonitor::start_recovery` | None | `src/execution/output_delivery_tests.rs`: exact success order, retry exhaustion with retained spool, nonretryable rejection, fresh replacement budget, cancellation/deadline no-loop rule, late lease failure after durable retirement and 1..8 policy bounds | Internal coordinator implemented. Each attempt consumes a newly opened output stream and a sealed reopener proves the same deterministic bytes. Only transport/dependency/spool availability is retryable; authorization and malformed output fail closed. A bound cancellation/deadline winner can replace the sole pending frame once, and the replacement receives a fresh bounded attempt budget. The path contains no fresh claim, Begin, input, authorization or ADK authority and remains capability-disabled until real TLS output and Redis clients are composed |
| Python worker `execution/delivery.py::_prepare_frame_settlement` and `control_grpc.py::ExecutionControlClient.prepare_settlement` | Fresh or restored terminal bound ACK and spool retirement precede deterministic PrepareSettlement; Redis retirement follows the validated receipt | `src/transport/output_grpc.rs::{OutputGrpcSession::send_terminal,PreparedOutputSpool::replay_terminal}`, `src/protocol/control.rs`, `src/transport/redis_commands.rs`, `src/execution/output_delivery.rs::{publish_pre_invocation_terminal,publish_agent_failure_terminal,recover_accepted_terminal}`, `src/execution/native_agent_lifecycle.rs` | None | Fresh bound-ACK/proposal-digest/receipt and restricted exact-delivery retirement component tests; restored-terminal ACK/missing-ACK proof; preparation/authorization/recovered coordinator `replay -> settlement -> Redis` order and failure-edge tests; native application/ad-hoc result, Stop and deadline retirement tests | Implemented behind internal capability gates for accepted local-terminal recovery, fresh pre-invocation/authenticated-authorization failures and the ordinary-text authorized native lifecycle. Fresh publication and byte-exact restored replay create the same consuming settlement proof only after the bound ACK has durably retired the local spool entry. Semantic control prepares the exact settlement, and the restricted retirement boundary consumes only the matching proof/delivery. Settlement failure never reaches Redis; Redis failure cannot regenerate or replay the already-ACKed terminal. Real TLS/ACL Redis and cross-process ACK-loss component tests remain gates |
| Python worker `execution/delivery.py::AgentExecutionDeliveryProcessor` and inherited `_process_admitted`; `serve.py::ProductionDeliveryProcessor` | Shared application/ad-hoc verification, all ten claim dispositions, recovery/settlement routing and authorize-once execution | `src/execution/{redis_delivery,agent_delivery,agent_delivery_processor,output_delivery,agent_preparation,agent_coordinator,agent_invocation,native_agent_lifecycle}.rs` | Invocation-exclusive Runner behind the Main effect fence; Elitea owns claim, spool, output ACK, settlement and Redis retirement | Redis task-group ownership tests, `tests/agent_delivery_route_contract.rs` and `src/execution/{output_delivery_tests,agent_preparation_tests}.rs`: both raw delivery entrypoints through retirement, stopped processor no-Begin/no-ACK, all ten claim classes, accepted-terminal replay/settlement/retirement ordering, spool-first ownership, shared fresh preparation, coordinator-owned authorization, stopped submission cleanup, Stop/deadline reduction and terminal retirement | Partial but end-to-end inside the disabled capability: `AgentDeliveryProcessor` now implements the Redis worker contract and owns raw delivery -> verify/claim -> output preflight -> prepare -> supervised authorize/native execution -> settlement/retirement. Its future awaits the coordinator waiter so the Redis runtime retains PEL heartbeat until the lifecycle closes; application and ad-hoc share the same injected control, retirement, clock, output, input and native composition. Exact accepted-terminal replay is also closed here. Active-lease/running/ambiguous claim recovery and pending-progress states remain deliberate no-ACK routes until the recovery coordinator lands. Normal bootstrap must stop/drain Redis deliveries before stopping/closing this coordinator; a hard drain timeout is process-wide, not permission to leave supervised work alive in a continuing process. Production config/TLS/signal bootstrap, deployed provider/session/tool construction, running-progress restart coordination and cross-process integration remain gates |
| Python worker `execution/sync_executor.py::{BoundedAdmissionGate,BoundedSyncExecutor.reserve,SyncExecutorReservation}` and `execution/delivery.py::_reserve_execution_capacity` | Bound running plus queued invocation work; reserve before `BeginExecution`; cancel/timeout/worker-drain without Begin/input effects; release unused capacity on every pre-submit path | `src/execution/invocation_admission.rs::{InvocationAdmission,InvocationReservation}`, `src/execution/{agent_preparation,agent_coordinator,agent_invocation,native_agent_lifecycle}.rs` | Native ADK execution consumes the opaque slot through the same supervisor-owned job; no Python thread-pool compatibility layer is reproduced | `src/execution/{invocation_admission_tests,agent_preparation_tests,output_delivery_tests}.rs`: exact bounds, capacity, cancellation, timeout/drain, saturation-before-Begin, dropped authorization waiter, stopped-coordinator return/close, unused release and real native-stream completion | Implemented through the capability-disabled native lifecycle. The fair bounded semaphore and explicit accepting state produce stable, data-free retry/no-ACK saturation or drain outcomes. The reservation is acquired before Begin, transferred synchronously by `AgentInvocationCoordinator` with the prepared authorization job, and retained by the same supervisor-owned operation until the real native event stream reaches EOS; output settlement continues under the remaining claim authority |
| Python worker `execution/sync_executor.py::BoundedSyncExecutor.run`, `execution/supervisor.py::ExecutionSupervisor` and process-drain handling in `serve.py` | Once submitted, real invocation work outlives a cancelled result waiter, retains its capacity slot until it exits and is joined during worker drain; process shutdown is not business Stop | `src/execution/agent_coordinator.rs`, `src/execution/invocation_supervisor.rs::{InvocationSupervisor,SupervisedInvocation}`, `src/execution/agent_invocation.rs::{AgentAuthorizationJob,AuthorizedAgentLifecycle}`, `src/execution/native_agent_lifecycle.rs`, `src/diagnostics.rs` | Tokio-owned task group around one invocation-exclusive ADK Runner | `src/execution/invocation_supervisor_tests.rs` and `src/execution/output_delivery_tests.rs`: dropped waiter, cancellation-safe drain, stop/submit race, coordinator rejection/cleanup, admission wake, foreign-pool rejection, redacted contained panic/resource release, invocation-scoped Stop and full settlement/retirement | Internal lifecycle implemented and capability-disabled. `AgentInvocationCoordinator::submit` is the synchronous ownership-transfer entrypoint, and every task consumes a reservation from the supervisor's exact bounded admission pool before `AuthorizeInvocation` can be polled. Stop closes that pool; a losing submission receives its original reservation and authority-bearing future intact for explicit close. A dropped waiter cannot cancel authorization, native execution or durable completion. Worker drain joins the operation without synthesizing business Stop; only the durable lease state requests the invocation-exclusive Runner to stop, and the lifecycle continues renewing and draining it to EOS before choosing terminal output |
| Python worker `capabilities.py::capability_message` | Capability identity and feature advertisement | `src/capabilities.rs` | None | Manifest golden and production-registration fail-closed test | Foundation: only empty production registration exists |
| Python worker `handlers/agent_events.py::CurrentAgentNodeEventCallback` | Ordered current-compatible events, pause projection, terminal browser artifact | `src/agents/events.rs::{AgentEventProjector,ProjectedAgentEventBatch,CompletedAgentBrowserOutput}`, `src/agents/graph/agent.rs`, `src/execution/native_agent_lifecycle.rs` | ADK `Event` is semantic input only; Elitea owns browser compatibility, identity and durability | `src/agents/events_tests.rs`: cumulative/delta text, thinking, tool call/result, direct sensitive pause/redaction, configured graph HITL, graph Toolkit/MCP confirmation and delegated-auth cards, terminal selection, identity substitution and fail-closed corpus; ordinary/pipeline tests prove original-call LLM authorization pause/resume/completion; output-delivery tests prove batch ACK backpressure, paused settlement and terminal retirement | Implemented capability-disabled for root text/tool events, direct sensitive confirmation, configured dynamic pipeline HITL, sensitive read-only Toolkit/MCP-node confirmation and delegated authorization from direct nodes, root `LlmAgent` calls and pipeline LLM nodes. LLM authorization cards expose no raw arguments; durable private state retains them for exact replay. Every projected batch is durably ACKed before ADK is polled again. Invocation/author/branch/event/part/codec bounds are enforced. A denied call closes with the structured same-call result; a direct graph node also preserves the separate SDK-formatted user explanation. Same-family parallel and nested authorization cards retain their exact hierarchy and admit only a bounded complete decision set. Tool progress, artifacts, provider errors, transfers/routes/escalation outside supported HITL, independent partial authorization decisions, mixed-guardrail aggregation, citations, compaction and unsupported non-root branches still fail closed. Skill metadata sanitization remains gated; raw LangChain/ADK custom JSON is never sent directly |
| `libs/proto/elitea/runtime/v1/node_event.proto`, Python `protocol/node_event.py`, and Main `runtimegrpc/nodeevent/codec.go` | Exact 13-field browser event, arbitrary JSON fragments, defaults, bounds and Go escaping | `src/protocol/node_event.rs`, closed rules in `src/protocol/wire.rs` | None | `tests/node_event_contract.rs`, existing two-case/36-type corpus, Python exact vectors and malformed/mutation cases | Implemented generic codec; unknown protobuf tags and noncanonical replay bytes intentionally fail earlier in Rust. Rust retains compact raw fragment number/escape spellings like authoritative Go `json.Compact`; Python currently normalizes those spellings while preserving semantics |

### Qualified model-tool binding

Current SDK `runtime/tools/tool_binding.py::{select_tools_for_binding,
build_tool_binding_plan}`, its `runtime/tools/llm.py` consumers and
`tests/runtime/test_tool_binding.py` define the observable compatibility rule:
persisted selection is toolkit-scoped, while a model provider receives one
flat namespace. Rust maps that rule to
`src/toolkits/tool_binding.rs`, root/nested assembly in
`src/agents/{ordinary,application_tools}.rs`, pipeline LLM assembly in
`src/agents/{pipeline,graph/llm}.rs`, and exact guard joins in
`src/agents/sensitive_tools.rs` and `src/toolkits/delegated_auth.rs`.

The Rust plan enumerates and freezes admitted toolsets once. A unique runtime
name remains unchanged. Every real collision receives a deterministic,
provider-safe `<toolkit>__<operation>` alias; a stable hash is added only for a
secondary/reserved collision or the 128-byte provider bound. The aliased ADK
tool delegates its declaration and execution to the exact original object.
Policy, sensitivity and delegated authorization therefore continue to use the
original `(toolkit, operation)` identity, while provider calls and confirmations
use the alias plus original call ID. Runtime-owned tools such as `ask_user` and
nested Application adapters reserve their exact names. Direct graph
Toolkit/MCP nodes never enter a flat model namespace and remain exact scoped
calls.

Property-style generated collision matrices cover ordering, character and
length bounds, reserved names and uniqueness. Full direct Application/ad-hoc
and pipeline-LLM component stories expose two MCP toolkits with the same
operation, call both aliases in one model response, and assert that each exact
underlying implementation and provider call ID reaches its own result. This
closes name isolation for admitted model-owned calls without claiming broader
MCP OAuth/DCR, catalog sync or production activation.

### ADK provider-version compatibility

The published ADK 2.1 provider changes are not inherited automatically because
Elitea implements claim-scoped, mTLS-bound, bounded and redacted transports
behind the provider-neutral `Llm` trait. The reviewed behavior maps as follows:

| ADK 2.1 provider behavior | Elitea implementation | Status / proof |
| --- | --- | --- |
| OpenAI and OpenAI-compatible function-call responses keep the agent turn open | `src/transport/openai_compatible_facade.rs::OpenAiStreamState` sets `turn_complete=false` for a terminal provider response containing tool calls | Adopted; the two-turn tool round trip asserts the original call remains open and the result is sent under the same call ID |
| Native Anthropic `tool_use` keeps the agent turn open | `src/transport/anthropic_facade.rs::AnthropicStreamState` separates provider-message completion from agent-turn completion | Adopted; the native tool round trip asserts the open turn, exact `tool_use_id`, result and final completion |
| GPT-5.6 Chat Completions tools need effective reasoning `none` when no effort was selected | `src/transport/openai_compatible_facade.rs::build_request_body` adds the provider-safe default only for `gpt-5.6*` plus a non-empty tool declaration set | Adopted; captured-request test proves Main's omitted effort becomes `none` without changing explicit profiles |
| Current Claude 5/Opus 4.7+ models reject explicit sampling | `src/transport/anthropic_facade.rs::native_generation` rejects the bounded known model set before request construction | Adopted; generated model cases prove zero transport calls |
| Python SDK `runtime/clients/client.py::get_llm` treats every non-empty reasoning string as enabled and therefore maps Anthropic `none` to a 4096-token legacy thinking budget (or an invalid adaptive effort) | `src/transport/anthropic_facade.rs::native_generation` treats the provider-neutral value as a disable operation and emits neither `thinking` nor `output_config` | Deliberate correction rather than byte-for-byte port; native request tests prove the configured visible-token limit and temperature are preserved for legacy models |
| Extended OpenAI reasoning values `minimal`, `xhigh` and `max` | Main application/profile schemas, Rust `parse_reasoning_effort`, and UI currently admit exactly `none`, `low`, `medium`, `high` | Deferred as one coordinated wire/UI change; the worker does not silently widen stored behavior |
| Updated provider catalog/pricing/defaults | Main configurations and Bifrost select models and own billing/provider policy | Not duplicated in the worker |
| Anthropic caller-selected headers/betas and server refusal fallback | Main redeems provider credentials and the Elitea gateway owns the upstream provider request; the frozen worker profile exposes no fallback policy | Deferred until Main can admit, audit and freeze an explicit profile; never enabled implicitly |

The ADK 2.2 delta is smaller: Vertex Gemini RAG support in `adk-model`, current
session-state access and drained-stream instrumentation in `adk-runner`, plus
package-level consolidation. The Rust dependency graph resolves all ADK crates
to 2.2.0, while the Elitea transport, session, checkpoint and claim owners stay
unchanged.

### MCP capability split

The current application has several independent MCP surfaces; the presence of
one must never be reported as completion of the others:

| Current source boundary | Business behavior | Rust status |
| --- | --- | --- |
| SDK `runtime/tools/function.py::{FunctionTool.invoke,_build_mcp_auth_interrupt,_build_mcp_auth_skipped_termination,_build_mcp_auth_refresh_termination}`, `runtime/utils/mcp_oauth.py::McpAuthorizationRequired`, `configurations/{sharepoint,openapi}.py`, `tools/openapi/{__init__,api_wrapper,tool}.py` and `runtime/langchain/langraph_agent.py` | A direct pipeline MCP node or configured Toolkit using delegated auth converts the common SDK exception into a native graph interrupt; Authorize rebuilds the selected family with returned credentials, while Skip nulls declared data outputs, preserves the formatted user explanation and routes to `END` | Implemented capability-disabled as a family-neutral control seam in `src/toolkits/{delegated_auth,mcp,invocation,materialize}.rs`, `src/toolkits/families/openapi/{config,spec,client,response_selection,tools}.rs`, `src/agents/graph/{direct_tool,resume}.rs`, and `src/agents/{ordinary,application_tools,pipeline,runtime,session,events}.rs`. RMCP 401/403 handling parses the bounded same-origin protected-resource challenge, resolves OAuth resource and authorization-server metadata under the toolkit timeout, and projects only bounded HTTPS endpoints and public capability lists. It then materializes only configured selected names as guarded placeholders. Configured materialization receives the same claim-scoped exact-resource token map and returns a merged auth catalog. The dynamic inline OpenAPI family exposes each selected operation's original name, description, and schema as a guarded tool and binds its canonical API base URL. Authorize replaces the guard with the real fixed-origin operation; Skip dispatches nothing. Both node kinds expose no tool arguments and bind the latest durable graph event, root plus nested checkpoints, pending node, definition, visit-specific call ID, canonical argument digest, and authorization resource. SharePoint runtime discovery, remote OpenAPI specifications, configured-family refresh adapters, and live provider proofs remain explicit family gates |
| SDK `runtime/tools/llm.py::{_build_mcp_auth_interrupt,_mcp_auth_decision_message}`, `runtime/toolkits/tools.py::{_build_deferred_mcp_auth_tools,_make_mcp_auth_control_tool}` plus `runtime/tools/application.py` and nested/parallel handling in `langraph_agent.py` | A model-owned tool loop pauses one or many exact delegated MCP or configured-Toolkit calls, exposes auth-control tools, then closes every original tool-call ID with a structured authorization result before the same model loop continues | Implemented capability-disabled for materializer-known remote-MCP and inline-OpenAPI challenges in root application/ad-hoc `LlmAgent`, nested direct-agent and pipeline LLM-node loops. Rust uses ADK `require_tool_confirmation` on each original guarded tool instead of asking the model to make a second `mcp_auth_control` call. Session/graph state retains the original call IDs and arguments. Authorize rematerializes each exact frozen resource with its claim-fetched token and replays without replanning; Skip returns SDK-compatible `mcp_auth_decision` under the same ID without dispatch. Complete same-family parallel/nested sets retain distinct hierarchy and resume independently executing leaves before the parent model continues. Authorization approval intentionally leaves any separate sensitive confirmation unresolved. This is an idiomatic behavioral port, not the Python workaround. Runtime-discovered auth from unported configured families may still need an adapter at their materializer boundary; independent partial sibling decisions and mixed guardrail sets remain gated. No synthetic model turn or generic error is accepted |
| Centry `api/v2/{mcp_oauth_proxy,mcp_dcr_proxy}.py`, `utils/mcp_oauth.py`, authorization request persistence in `utils/toolkit_authorization.py`, and continue routing in `rpc/chat_all.py` | Browser OAuth, token exchange, DCR, exact authorization-card persistence/retirement and nested decision routing | Platform-owned. Rust consumes only claim-fetched tokens and sanitized challenge metadata; it neither performs DCR nor stores OAuth credentials. Main's `agent.continue.authorization.v1` route admits one exact scalar decision or one complete bounded `hitl_decisions` set and atomically matches current authorization IDs. The web client now uses its existing OAuth/DCR proxy flow, submits the resulting credential map with the exact request ID, and batches every pending sibling decision. Independent partial submission and mixed guardrail aggregation remain later contracts |
| Current EliteaUI `[fsd]/features/chat/lib/helpers/mcpAuthorization.helpers.js`, `[fsd]/features/mcp/lib/helpers/mcpAuth.helpers.js`, and `[fsd]/features/mcp/lib/hooks/useMcpTokenChange.hooks.js` | Render one authorization action per exact interrupt, share one credential across matching parallel requests, and invalidate a logged-out credential in every browser tab | `apps/elitea-web/src/entities/message/lib/authorizationActions.ts`, `features/chat-messages/lib/chatStreamToolFrames.ts`, `features/mcps/lib/{storage,logoutSync}.ts`, `features/mcps/model/useMcpTokenChange.ts`, and `widgets/chat-box/ui/hooks/useChatBoxHandlers.authorization.ts` implement the same business behavior. Live and persisted terminal metadata produce deduplicated cards. One successful OAuth flow authorizes all pending requests with the same storage key; unrelated credentials wait for their own decision. The final continuation uses exact IDs and the current token map. A credential-scoped local-storage tombstone invalidates older tab-local session tokens, including same-millisecond reauthorization races. Token values and private OAuth fields never enter cards or traces |
| Centry `rpc/application.py`, `api/v2/{mcp_sync_tools,toolkit_discover_tools}.py`, `methods/mcp_prebuilt_config.py`, `utils/internal_tools.py` and MCP server storage/catalog methods | Discover/sync external tools, resolve prebuilt definitions, inject only trusted internal Elitea MCP endpoints and project registered servers into toolkit catalogs | Not a worker-execution completion claim. Fixed and declared-parameter catalogue-backed HTTP definitions enter Rust after Main claim-time resolution. Main now injects runtime-only project identity and a current-user PAT only for its configured same-origin `/app/{project}/mcp/...` endpoints. The first explicit internal builder category, `elitea_core/applications`, executes eight Main-owned operations through Rust's existing configured-MCP client. Descriptor sync, stdio, other internal categories and external Elitea-as-MCP publishing remain separate gates |
| Centry `routes/mcp_sse.py`, `methods/mcp_sse.py`, `utils/mcp_service.py` and `utils/mcp_servers_storage.py` | Expose selected Elitea toolkits and applications/agents as MCP server tools, own SSE sessions, registration and periodic server validation | Separate MCP-server/publishing family; not implemented by this client/auth slice. It needs its own identity, project/user authorization, streaming, tool-call, lifecycle and deployment mapping before Rust activation |

The direct-node continuation intentionally uses the existing
`AgentExecutionInputV1` fields and does not change Main or the Python-worker
contract. The exact browser authorization ID remains authoritative in Main;
Rust's latest-event plus server/checkpoint join is a bounded bridge, not the
final partial-sibling and mixed-guardrail delegated-auth contract.

### Browser SSE envelope

The current UI contract is LangChain-inspired but Elitea-owned; workers do not
send raw LangChain or ADK custom-event JSON to browsers. Python
`CurrentAgentNodeEventCallback._emit` first builds the bounded current
`NodeEventV1` JSON object. Rust encodes the same protobuf inside a nonterminal
`ExecutionOutputFrameV1`. Go Main then:

1. validates and encodes the protobuf in
   `internal/transport/runtimegrpc/nodeevent/codec.go`;
2. claim/fence-checks and atomically stores the browser JSON as an
   `execution.node_event` replay row in
   `internal/infra/db/repos/node_events.go`; and
3. writes `id: <cursor>`, `event: execution.node_event`, and the compact current
   NodeEvent JSON as the SSE `data:` value from
   `internal/api/v2/executions/events.go::writeDurableEvent`.

ADK graph `TaskContext::emit(StreamEvent::custom(...))`, `NodeOutput.events`,
and `ToolContext::emit_progress` are useful semantic inputs, including for the
later indexing slice. They still require an allowlisted Rust adapter before
this data plane. In particular, functional `TaskContext::emit` uses Tokio
broadcast and has no durable/backpressure guarantee, so it cannot replace the
Elitea spool, Main ACK, replay repository, or SSE cursor.

### Intentional NodeEvent codec deviations

The committed Python and Go codecs disagree on noncanonical-but-valid JSON
fragment spellings. Python parses then serializes, so `"\u0061"`, `1e0` and
`-0` become `"a"`, `1.0` and `0`. Main's Go codec uses `json.Compact` and
preserves the original spellings. Rust deliberately follows the Go durable
consumer: it validates and compacts fragments without float coercion or string
normalization. Browser semantics are unchanged, but semantically equal input
from Python and Rust can have different protobuf bytes and payload digests.
`tests/fixtures/node_event_vectors.txt` and
`go_authoritative_raw_fragment_policy_is_explicit_against_python_normalization`
freeze this disagreement explicitly; the future event bridge must construct
stable fragments once and rely on spool replay rather than regenerate an
already allocated sequence.

Rust also classifies byte, depth, decoded-string, protobuf and complete-frame
limit failures as `ProtocolError::ResourceExhausted`. The current Python codec
collapses most of these into `InvalidCurrentNodeEvent`, which its delivery
bridge maps to `INVALID_INPUT`. The typed Rust classification is intentional;
the future UI compatibility bridge may map it to the legacy public error only
where the existing product contract requires that presentation. Neither
deviation is a claim of byte-for-byte Python parity.

The agent result's nested artifact message is only an immutable reference. Its
digest is distinct from the output frame payload digest, and current Main does
not perform the index-ingest path's durable artifact verification for agent
results. Terminal frame construction therefore does not claim that artifact
bytes are durable. Production registration stays disabled until Main owns that
verification/commit boundary or an equivalent contract is introduced and
tested end to end.

The authoritative Go admission contract and the Python worker constant both
set the agent-execution input limit to 1 MiB, and Rust follows that limit in
`src/protocol/control.rs`. The current Python content HTTP configuration and
shared fixture projector still cap all materialized bodies at 256 KiB. This is
a recorded source drift, not simultaneous parity: production enablement must
either raise the Python-era data-plane limit to the admitted 1 MiB or tighten
the language-neutral contract consistently. Rust does not silently inherit the
shared settings limit for agent protobuf input.

Rust also rejects rejection responses that simultaneously carry success
disposition, lease/state or settlement receipt fields. Python already enforces
this for Begin/Authorize but not every control response; Main emits exclusive
shapes today. The stricter sum-type interpretation is deliberate protocol
hardening and is covered by mutation tests.

### Output spool deployment and rotation gates

The compatible disk format is `ELITEASPOOL1\0`, a 12-byte random nonce and
AES-256-GCM ciphertext/tag. The entry AAD binds the seven-field execution
identity and big-endian sequence. Rust preserves that format so a drained
Python replica can be restarted by Rust with the same root and master key.

One persistent spool root belongs to exactly one worker replica, and one
execution child has exactly one live process owner. Rust holds a nonblocking
advisory directory lock as defense in depth, but the Python worker does not
participate in it and network/FUSE lock semantics are not a correctness
boundary. Mixed-language rollout therefore uses distinct replica roots or a
strictly exclusive lifecycle on local/block-backed persistent storage.

Redis reclaim and Main's claim, lease and fence transitions are the control
plane for assigning an abandoned command to a replacement worker; Rust's fast
startup only reduces one part of that recovery latency. If the abandoned
execution has pending output, the replacement may proceed only after the prior
owner is fenced out and the same persistent spool root is exclusively
reattached. A StatefulSet-style replica identity with one reattachable volume
per replica is the expected deployment shape. Ephemeral pod-local storage
cannot satisfy this recovery contract, and a concurrently mounted shared RWX
directory is not a substitute unless its locking and failure semantics have
separately been proven. An uncertain authorized invocation is never made safe
by fast restart: the replacement follows Main's typed recovery disposition and
must not blindly enter ADK again.

The v1 file has no key identifier. Rotating the 32-byte master key while any
encrypted entry remains would make recovery impossible. Production wiring must
either drain every replica root before rotation or introduce and test a
versioned keyring/file revision. This primitive deliberately implements
neither silent fallback nor multi-key trial decryption.

`SpoolMasterKey` and the explicit 32-byte HKDF expansion buffer are zeroized
when their Rust owners release them. `ring` 0.17.14 does not document
zeroize-on-drop for its internal HKDF PRK or installed AES key schedule, so
this slice does not claim complete post-drop key erasure from worker process
memory. Process isolation and termination remain the memory-disposal boundary;
a future stronger in-process erasure requirement must select and audit a
cipher/KDF representation which guarantees it.

### Output stream boundaries and deviations

`src/transport/output_grpc.rs` owns one ordered stream attempt. It receives and
validates bootstrap credit before any write, uses tonic's bounded request
channel, persists exact deterministic protobuf bytes on the blocking pool,
consumes absolute frame/byte credit, waits for the bound ACK, deletes and
fsyncs the acknowledged spool prefix, and only then advances the caller-visible
watermark. Transport failure latches the attempt unusable; the delivery layer
will create a fresh stream and a fresh exclusive spool handle for bounded
replay, matching Python's retry ownership rather than reconnecting secretly.

`PreparedOutputSpool` validates and exposes the encrypted durable state before
any endpoint is contacted. Delivery coordination must choose exact replay,
complete-prefix reconciliation, same-binding terminal replacement, or one of
the two narrowly validated fresh-fence recovery replacements before consuming
the object to connect. Appending after a terminal and reusing a reconciled
object are rejected by the Rust state machine.

An owned per-execution actor continues an admitted durable send even when its
caller stops awaiting it, preventing delayed ACK reassignment. Its admission
channel and byte permit allow only one current frame; explicit close interrupts
credit, send, or ACK waits and joins the actor before releasing the spool lock.

Production Main grants one frame and 64 KiB. Rust therefore serializes each
execution's `spool -> send -> ACK` operation. Tonic/hyper independently drives
HTTP/2 request and response I/O, so the first send enters an empty bounded
channel and ACK receipt is not coupled to a blocking socket write. This is an
intentional simplification of Python's separate writer/receiver task pair for
the current one-frame window; any future server policy with multiple in-flight
frames requires a new bounded-concurrency proof rather than silently changing
this state machine.

Although protobuf counters are unsigned, Main stores sequence, generation,
claim attempt, lease epoch and handoff watermark in PostgreSQL `BIGINT`. Rust
rejects values above `i64::MAX` before transport. This narrows the Python
session's nominal `u64` acceptance to the actual durable consumer domain.

The output channel is injected, not constructed from TLS files in this slice.
The generated tonic client is explicitly capped at 64 KiB encoded requests and
80 KiB decoded ACKs, and sends the two allowlisted workload metadata values
once. Production composition must still load private material safely, require
TLS 1.3 and verified client/server identities, and prove hostname/CA failure
cases. Passing a channel is not production admission authority.

Tonic's generated prost response decoder does not expose erased unknown or
duplicate ACK fields for a second strict-wire scan. Current Main is mutually
authenticated and emits canonical ACKs, so exact decoded binding and semantic
validation is the current compatibility boundary. Symmetric raw-wire rejection
would require a reviewed custom tonic codec and remains a production-hardening
decision, not a claimed property of this slice.

The control transport follows the same composition boundary. It clones tonic's
cheap HTTP/2 client handle per call so lease polling and delivery fencing do not
share a mutable request bottleneck, applies a positive deployment deadline of
at most five minutes, and performs exactly one attempt. It never interprets a
claim, begin, invocation, lease, desired-state, or settlement response as
authority; the typed semantic validator and delivery state machine own those
decisions. The current semantic client consumes authenticated one-attempt RPC
results behind non-forgeable claim, lease, invocation and terminal-settlement
typestates; callers cannot mint those proofs from decoded protobuf values.

### Error and tracing boundary

Runtime modules return typed errors with a stable low-cardinality code,
retryability, a data-free `Display` message and the original error as `source`
where doing so is safe. This keeps terminal/no-ACK decisions deterministic
while still giving an operator a useful causal chain. Server-provided text,
response bodies, prompts, MCP tokens, fence tokens, claim credentials and
materialized input never enter the public message or structured fields.

Tracing begins at orchestration boundaries rather than inside every protocol
helper. The worker emits redacted structured logs and standard OTLP spans for
command verification, Redis delivery, claim, preparation, native ADK execution,
model requests, session/checkpoint writes, output, settlement and retirement.
Fresh preparation emits `agent.prepare`; later phases include
`agent.delivery.verify`, `agent.delivery`, `agent.native_lifecycle`,
`agent.model.request`, `agent.session.persist` and graph-checkpoint spans.

Valid W3C `traceparent` metadata continues the upstream trace.
`ELITEA_RUST_LOG` and `ELITEA_RUST_TRACE` accept one level from `off` through
`trace` and remain crate-scoped. They cannot enable dependency-owned HTTP,
model, SQL or SMTP fields. Execution identifiers are correlation fields, not
metric labels. Metrics may use only bounded kind, outcome and error-code values.
Prompts, arguments, results, URLs, credentials, tokens and provider bodies are
never fields.

The process installs a redacted panic hook before runtime composition. It hides
the panic payload and full source path, but retains a sanitized filename and
line for diagnosis. OTLP batching uses the Tokio runtime. This avoids the
exporter-thread reactor panic that aborted the release process when the async
HTTP exporter flushed on a non-Tokio thread. Backend retention, sampling,
metrics and Kubernetes activation remain deployment-owned.

### Agent input field projection

Every wire field is declared in `libs/proto/elitea/runtime/v1/agent.proto`,
decoded by Python `protocol/agent.py::request_from`, and now owned by the Rust
types below. This is a field projection, not dispatch authority: fences and
runtime clients never become request fields. `mcp_tokens` is claim-fetched
secret data, so the Rust payload intentionally implements neither `Clone` nor
`Debug`.

| Proto fields | Python projected payload | Rust projection and validator | Status |
| --- | --- | --- | --- |
| `schema_revision` | validated before `request_from` | `protocol.rs::parse_agent_execution_input` | Implemented |
| `llm`, `chat_history`, `user_input` | `AgentExecutionPayload.{llm,chat_history,user_input}` | `request.rs::AgentExecutionPayload`; object/list/text-or-block validation in `protocol.rs` | Implemented |
| `thread_id`, `checkpoint_id`, `debug` | same names | `request.rs::AgentExecutionPayload` with protobuf presence preserved | Implemented |
| `tools`, `application`, `internal_tools`, `steps_limit` | same names | owned JSON/list/string-list/positive `u32`; application/ad-hoc identity checks in `protocol.rs` | Implemented |
| `mcp_tokens`, `ignored_mcp_servers`, `user_declined_mcp_servers` | same names | owned object/list fields in `request.rs`; credentials remain claim-fetched and are never logged or retained in authorization metadata | Implemented wire parity plus capability-disabled exact-resource rebuild for remote MCP and bounded inline OpenAPI across direct graph, root/nested `LlmAgent` and pipeline LLM-node owners. SharePoint, runtime-discovered configured families, independent partial decisions and remaining OAuth/content capabilities remain gated |
| `should_continue`, `hitl_resume`, `hitl_action`, `hitl_value`, `hitl_decisions` | same names | presence-preserving fields plus resume-decision invariant in `protocol.rs`; exact direct LLM-tool and graph Toolkit/MCP/HITL decisions in `direct_hitl.rs`, `graph/resume.rs` and `runtime.rs` | Implemented capability-disabled for direct sensitive-tool continuation including complete parallel nested direct-agent decision sets, configured graph HITL, sensitive read-only direct Toolkit/MCP-node continuation, and complete same-family delegated-authorization sets in direct graph, root/nested `LlmAgent` and pipeline LLM-node owners. Independent partial sibling decisions, mixed-guardrail aggregation, effects and unported delegated Toolkit families remain gated |
| `execution_generation`, `is_regenerate`, `meta`, `conversation_id` | same names | owned immutable fields in `request.rs` | Implemented input parity |
| `persona`, `context_settings`, `supports_vision`, `return_chat_history` | same names; empty persona becomes `generic` | same fields and default in `request.rs`/`protocol.rs` | Implemented input parity |
| `invoked_skills`, `applied_skills`, `attached_skills` | same names | owned JSON lists in `request.rs` | Implemented input parity; skill runtime planned |
| `auto_approve_sensitive_actions`, `input_attachments` | same names | owned fields in `request.rs` | Implemented input parity; policy/effect runtime planned |
| `parallel_reconcile`, `parallel_terminal_errors` | same names | optional object/list in `request.rs` | Implemented decoding only; end-to-end behavior blocked |
| `exception_handling_enabled`, `debug_mode` | same names | `Option<bool>` in `request.rs` | Implemented |
| `next_input_suggestion` | bounded policy object with defaults | typed `NextInputSuggestionPolicy` and closed-field validation | Implemented |

## Current SDK client ownership

`elitea_sdk/runtime/clients/client.py` contains several unrelated authorities.
Rust splits them by capability instead of recreating one broad client:

| Current SDK symbol | Current behavior | Rust owner | Status / deliberate change |
| --- | --- | --- | --- |
| `EliteAClient.get_llm` | Builds OpenAI-compatible or native Anthropic clients from model settings and the execution actor | `src/transport/model_facade.rs`, with bounded wire adapters in `openai_compatible_facade.rs` and `anthropic_facade.rs` | Foundation implemented. `ModelFacade` is provider-neutral and receives the frozen `model_project_id`; Rust uses that ID for `OpenAI-Organization` while retaining the claim actor token. The SDK currently substitutes the execution project here, which is wrong for an admitted shared/project-bound model |
| `EliteAClient.get_app_version_details` | Performs a live mutable `PATCH` through the public SDK client with bearer plus `X-SECRET`, then rebuilds a child on each call | `src/transport/platform_client.rs`, `src/transport/runtime_context.rs`, `src/agents/application_tools.rs` | Foundation implemented behind a missing Main route. Rust asks for one exact `(application_id, version_id)` through the live claim and retains that resolved definition for the assembly; it never falls back to the mutable public read. Main does not yet expose the claim-bound frozen/materialized route, so nested production activation remains closed. If Main later supplies the complete frozen child definition in the secure execution input, this lookup and gap disappear. Future artifact grants also belong to `PlatformClient`; model traffic does not |
| `EliteAClient._inject_summarization` | Optionally injects token-triggered summarization, usually using a low-tier model, and reports callbacks against a conversation | `src/agents/context_management.rs`, `src/agents/session.rs`, `src/state/postgres_session.rs`, and graph-only `src/state/postgres_checkpointer.rs` | Active for direct and ad-hoc agents. Main's frozen contract is admitted before PAT redemption and composed onto the Runner as a `CompactionConfig` whose strategy reproduces the SDK's `SummarizationMiddleware.before_model` ordering over ADK's `LlmEventSummarizer`. Compaction runs per invocation through `MutableSession::replace_events`, so the PostgreSQL `SessionService` keeps the complete transcript and a resume recompacts it; `EventsCompactionConfig` is deliberately unused because its persisted markers would be irreversible. Default stays off end to end. Browser analytics projection, tool-output editing, a dedicated summary model and pipeline-graph context management remain closed |
| `EliteAClient._inject_context_editing` | Optionally clears older tool outputs while preserving recent context | Same context-management owner plus the event/checkpoint projection | Planned inside the same seam. Tool call/result pairing, nested-agent isolation, HITL resume and post-compaction skill state must be preserved; no destructive transcript editing is enabled yet |

## Assembly, execution, and state

The current platform already transports delegated-execution hierarchy in output
event metadata; Rust must not reconstruct it from checkpoints. The SDK
`runtime/tools/application.py` appends each exact wrapper invocation to
`parent_agent_path` and propagates `parent_agent_call_id`, while
`runtime/tools/llm.py` adds the per-call `sibling_ordinal` for parallel
delegation. The Python worker preserves the same fields through
`utils/agent_execution_common.py`, `utils/node_interface.py` and
`methods/agent_common.py`. EliteaUI
`executionHierarchy.helpers.js` treats the call ID as invocation identity and
the ordinal as display metadata; its grouping helpers keep ordinary pipeline
nodes flat by `langgraph_node`/`checkpoint_ns` and create nested accordions only
for delegated Application/Pipeline wrappers. The Rust projector therefore emits
the existing wrapper plus inherited `parent_agent_path` contract and retains
`langgraph_node` on inner pipeline-node events. No second breadcrumb schema or
hierarchy table is introduced.

| Python source path and symbol | Observable behavior | Rust target | Native ADK primitive or Elitea ownership | Proving tests | Status / deviation |
| --- | --- | --- | --- | --- | --- |
| Python worker `execution/delivery.py::_execute_resolved` and `handlers/agent.py::AgentExecutionHandler.execute` | After durable authorization, select exactly one application/ad-hoc runtime entry and stream its semantic events through end-of-stream | `src/execution/{agent_delivery_processor,agent_coordinator,agent_invocation,native_agent_lifecycle}.rs`, `src/agents/{native_runtime,assembly,ordinary,pipeline,runtime,events,session,sensitive_tools,direct_hitl,application_tools}.rs`, `src/agents/graph/{application,compiler,resume,hitl,state_modifier,llm,direct_tool,printer,router,decision}.rs`, `src/state/{postgres_session,postgres_checkpointer}.rs`; `src/execution/agent_preparation.rs::AuthorizedAgentRun`; dependency pins in `Cargo.toml` | Exact ADK-Rust `2.2.0` `Agent` + invocation-exclusive `Runner` + injected `SessionService` entrypoint, with direct `LlmAgent` or graph agent plus common model/tool facades behind the Elitea authority boundary | Native primitive, raw-delivery, runtime, graph/compiler/node, lifecycle/output and PostgreSQL state corpus including direct and graph sensitive pause/no-call, exact read approval, same-ID denial, restart/replay rejection, direct Agent and saved-pipeline Subgraph execution, bounded deep dynamic HITL, LLM/Toolkit/MCP/Router/Decision/state projection and terminal completion | Capability-disabled common entrypoint is implemented from a raw Redis delivery through direct text, exact sensitive-tool continuation and stored pipelines composed from Agent, dynamic HITL, state modifier, native LLM, configured Toolkit, auth-free remote MCP, Printer, Router and Decision nodes. An Agent node invokes a direct saved agent through native `AgentTool` or an exact saved pipeline through an isolated checkpointed native `SubgraphNode`; dynamic child HITL binds and validates a bounded exact descendant checkpoint chain before one-use resume through every pending parent. `AgentDeliveryProcessor` and `AgentInvocationCoordinator` install the same lifecycle owner around the strict direct/pipeline assembler. Application/ad-hoc direct agents share authorization, Runner, event backpressure, lease/deadline, settlement and Redis retirement; stored pipelines use the same Runner/SessionService plus a Checkpointer, without fake one-node graphs. Runtime policy binds LLM-owned calls to native confirmation and direct graph Toolkit/MCP reads to checkpointed dynamic confirmation. Approval yields normal output; graph denial stores the same-call structured result plus formatted user chat explanation and stops at `END` with clean typed state. Router performs deterministic bounded state selection; Decision receives a no-tool claim-bound model and can choose only declared targets. Final-response events do not terminate early: lifecycle drains real EOS. Production config/TLS/signal bootstrap, input-free running/ambiguous recovery, pipeline continuation-suffix recovery, approved-effect receipts, rich-history cutover, recursively materialized child participants, nested static Printer pauses, Code nodes, skills, platform OAuth/DCR, runtime-discovered auth refresh, remaining configured-family gaps and deployed construction remain closed |
| SDK `runtime/clients/client.py::EliteAClient.application` | Saved direct-agent version, model, tools, nested applications, variables and middleware policy | `src/agents/{assembly,ordinary,session,application_tools}.rs`, `src/toolkits/{snapshot,materialize}.rs` | Direct `LlmAgent`/Runner and ADK toolsets/`AgentTool`; Elitea owns admitted snapshots, recursion and policy | Strict saved-agent fixture, ordered frozen text history, configured-tool E2E, nested exact-version parent/child model loop and bounded repeated-participant fan-out | Partial capability-disabled direct-agent path: model controls, text history, configured native toolsets and variable-free nested `agent_type=agent` children are assembled. When every frozen root tool is an Application, ADK dispatches one model batch concurrently with an eight-call ceiling and restores provider call order; mixed toolsets stay sequential. Pipelines stay with the graph compiler; variables/templates, internal/lazy tools, independent partial authorization resume, effects and context editing, a dedicated summary model and pipeline-graph context management remain closed |
| SDK `runtime/clients/client.py::EliteAClient.predict_agent` | Ad-hoc model/instructions/history plus configured toolkit, MCP and selected application/pipeline participants | `src/agents/{assembly,ordinary,session,events,application_tools}.rs`, `src/toolkits/{snapshot,materialize}.rs` | One direct `LlmAgent`; native toolsets and child `AgentTool` values are attached without a one-node graph | Strict ad-hoc fixture, ordered history, stable pseudonymous session identity, real configured-tool loop, nested saved-agent loop, concurrent same-participant calls and UI hierarchy projection | Partial capability-disabled path: native configured toolsets and variable-free saved agents work through the same direct assembler. A pure Application tool set opts into bounded native parallel dispatch; each wrapper event uses the provider call ID as stable invocation identity and carries frozen display metadata plus a sibling ordinal. Stock `AgentTool` buffers nested events, so the typed Application adapter forwards them through the root session; recursive call-bound hierarchy and parallel sensitive-tool HITL aggregation are implemented, while independent partial authorization resume remains gated. Pipelines-as-tools/participants, non-generic personas, skills and rich content remain later slices |
| SDK `runtime/langchain/assistant.py::Assistant` | Agent, pipeline, swarm, internal-tool and lazy-tool selection | `src/agents/assembly.rs`, `src/agents/swarm.rs` | Use ADK agents/toolsets/handoffs where compatible | Mode-selection and shared-history tests | Planned |
| SDK `runtime/toolkits/tools.py::get_tools` and `runtime/lazy_tools.py::ToolRegistry` | Materialize selected configured tools, application tools, MCP, metadata and policies | `src/toolkits/{snapshot,materialize,delegated_auth,mcp,invocation,policy}.rs`, `src/toolkits/families/openapi/`, `src/agents/application_tools.rs` | ADK `Toolset`, `BasicToolset`, MCP toolset and `AgentTool`; Elitea owns frozen configuration, schemas, selected-tool identity, sensitive policy and bounds | Complete-family catalog suites plus configured, mixed-supported/unsupported, MCP, OpenAPI and saved-child direct-agent loops | Partial: configured native families, fixed-origin saved HTTP MCP, dynamic inline OpenAPI and direct saved agents enter the same LLM tool loop with selection-oriented descriptions and generic bounded tracing. Direct sensitive reads resume through the durable root session, including recursively nested direct agents. Materializer-known MCP/OpenAPI delegated auth pauses/resumes exact model-owned calls in root and pipeline loops as well as direct nodes; same-family nested cards aggregate with exact hierarchy and Main consumes a bounded complete set. A participant remains attached when its dynamic schema or frozen family is unavailable; Main or Rust omits only that runtime toolset and emits `agent_toolkit_skipped`. Invalid supported-family configuration, authorization and dependencies remain fail-closed. Lazy tools, independent partial resume, remaining SharePoint/OpenAPI capabilities and approved effects remain closed |
| SDK `runtime/tools/ask_user.py::{AskUserTool,AskUserToolkit}`, `runtime/toolkits/tools.py::get_tools`; Main current application/ad-hoc input and continuation routes; EliteaUI clarification card/composer | Expose one runtime-selected `ask_user` tool with model-supplied question data, pause durably for 1-4 normalized questions, accept an object or typed string answer, and continue the existing model tool loop with `User answered...` under the original call ID | `src/agents/internal_tools.rs`, `src/agents/{assembly,ordinary,session,application_tools,direct_hitl,events,pipeline}.rs`, `src/agents/graph/{llm,resume}.rs`; Main `internal/application/agentexecution/{start,adhoc,continue}.go`, `internal/api/v2/agentexecution/route.go`, `internal/infra/db/repos/agent_start.go` and `internal/db/{queries,sqlcgen}/agent_chat.sql*` | Runtime-owned ADK `Toolset` plus native `require_tool_confirmation`; direct agents persist ordinary confirmation events in `SessionService`, while pipeline LLM nodes use the existing dynamic graph interrupt and Checkpointer. Elitea owns normalization, public `clarifying_question`/`answer` contract, exact-call replay and bounds | Internal-tool normalization/format tests; direct-agent pause/project/restart/same-call result E2E; pipeline LLM checkpoint/card/answer/same-call provider transcript E2E; nested-parallel saved-child distinct-card/atomic-answer/exact-replay E2E; Main route canonicalization and application/ad-hoc projection tests | Implemented capability-disabled. Model-facing arguments may omit IDs and use source snake-case fields; Rust canonicalizes them to stable `q1`... IDs for the UI while binding the raw call through its digest. Resume replaces only the original `ask_user` call with the SDK-compatible string result and marks the ADK confirmation approved. It never executes the fail-closed placeholder, injects the answer as a user message, replans the selected call, or adds an interrupt table. Main keeps protobuf v1 unchanged by carrying canonical JSON in the existing string value. Nested direct agents inherit the same tool/catalog and hierarchy. Two parallel calls to one saved child retain distinct hierarchy/checkpoint identities, reject partial answer sets, revalidate the rematerialized child's frozen internal-tool scope, and resume with separate answers. Other internal tools remain gated below |
| SDK `runtime/langchain/utils.py::{create_state,_hitl_decisions_reducer,_parallel_tasks_reducer}` and `langraph_agent.py::{create_graph,PrinterNode,ConditionalEdge,DecisionEdge,LangGraphAgentRunnable.invoke,extract_terminal_state_output,extract_state_fallback_output}` plus `runtime/tools/{application,router}.py` and Python worker application dispatch | Select a frozen stored pipeline, compile its YAML, preserve pinned state/reducer and active node/edge semantics, and complete, publicly pause or resume through one runtime entrypoint | `src/agents/{native_runtime,pipeline,session,runtime,events,application_tools}.rs`, `src/agents/graph/{application,compiler,resume,yaml,parallel,hitl,state_modifier,llm,direct_tool,printer,router,decision,agent}.rs`; active/deprecated inventory in `docs/source-mapping/pipeline-nodes.md` | ADK `GraphAgent`, `StateSchema`/reducers, custom `Node`, nested `LlmAgent`, `AgentTool`, isolated `SubgraphNode`, native `ToolContext`, dynamic interrupts, compiler-owned `interrupt_after`, atomic `goto`, `Runner`, `SessionService` and `Checkpointer` behind Elitea YAML, claim and browser adapters | Strict admission, complete-document/restart, Agent/parallel/HITL/state-modifier/LLM/Toolkit/MCP/Printer/Router/Decision YAML, exact participant/action projection, sensitive/effect no-call, reducers, root and nested graph interrupt/checkpoint resume, public-result and root-identity fixtures | Initial capability-disabled selector, assembler and compiler are implemented for exact saved direct Agent and saved-pipeline participants, dynamic HITL, state-modifier, native LLM, configured Toolkit, auth-free remote MCP, Printer, Router and Decision nodes. Agent nodes map one task into either a claim-bound saved direct agent or an exact saved pipeline, append its response and project declared outputs. The child pipeline is an isolated native `SubgraphNode` over the same Checkpointer; it receives only explicit input/resume channels and returns only `elitea_response`. Dynamic nested HITL pauses the parent, privately binds both checkpoint identities and resumes the same child invocation once without exposing its thread. Each LLM node receives only its UI-selected configured/MCP tool names; materializer-known remote-MCP challenges pause and replay the original call after exact token rebuild. Each direct Toolkit/MCP node resolves one exact selected read and calls it once without a model turn. One claim opens both durable state adapters behind the common Runner. The pseudonymous session is the private root checkpoint thread; ADK derives a namespaced child thread, while the public chat thread remains output metadata only. `messages` is a bounded JSON business channel with the ADK append reducer. Configured graph HITL is distinct from sensitive-tool confirmation; direct Toolkit/MCP confirmation carries its visit-specific tool-call ID and stops at `END` when denied. `START`/`END` remain control sentinels. Production config/TLS/signal bootstrap, continuation-suffix recovery, recursively nested pipeline Agent nodes, child variables, nested static Printer pauses, Code nodes, independent partial authorization decisions and stdio remain gates |
| EliteaUI `LLMNode.jsx`, `useLLMInputMapping.js`; SDK `runtime/tools/llm.py::LLMNode`, `runtime/langchain/utils.py::propagate_the_input_mapping` | A pipeline LLM maps fixed/variable/fstring system, task and history inputs; exposes only node-selected tools; appends its assistant message; and either writes one plain output or maps structured keys into declared state | `src/agents/graph/{llm,node_events,resume,compiler}.rs`, `src/agents/{pipeline,session,events}.rs`, `src/toolkits/{snapshot,delegated_auth}.rs` | Fresh node-scoped ADK `LlmAgent`, native tool loop, `output_schema` and `require_tool_confirmation`; Elitea owns frozen mapping, exact tool authority, bounded progress bridge, checkpoint join and typed state projection | LLM-node and pipeline/compiler tests cover UI and legacy mappings, UTF-8 text blocks, zero-tool scope, missing/duplicate/blocked/sensitive tools, structured/plain projection, a compiled checkpointed graph, node-labelled model/tool progress, root Block With Comment replay, delegated authorize/skip/sensitive separation and saved-pipeline child HITL/same-ID denial without real-tool dispatch or provider replanning | Implemented capability-disabled. Rust keeps the observable checkpoint `messages` channel but deliberately does not port the Python custom runnable, second coercion model call, mutable wrappers, implicit hidden tools, flattened history or warning-and-skip missing-tool behavior. Empty aliases cannot redeem credentials. A private 512-KiB-bounded graph-interrupt/session envelope retains the exact pending ADK turn and accumulated call-ID decisions; resume places the resolved value in a reserved graph-state channel, while its public card masks raw arguments. Approval returns the ordinary result. Reject and Block With Comment send `sensitive_tool_blocked` to the native model loop under the original call ID with no synthetic user turn. Materializer-known remote-MCP authorization uses the same envelope: Authorize rematerializes the exact call and can pause again for sensitivity, while Skip returns `mcp_auth_decision` under the same call ID with no dispatch. A 64-slot invocation-local channel forwards sanitized native model/tool lifecycle events with `langgraph_node` identity and no graph-state writes; the selected graph result remains the sole terminal output. Application references, multimodal/tool-result input history, approved-effect receipts, independent partial authorization decisions and incremental tool-progress chunks remain fail-closed gates |
| EliteaUI `AgentNode.jsx`; SDK `runtime/tools/application.py::Application` and `langraph_agent.py` Agent branch | Invoke a selected saved application or pipeline using one mapped `task`, then append and project the final child response | `src/agents/graph/{application,compiler}.rs`, `src/agents/{application_tools,pipeline,session,events}.rs`, `src/transport/platform_client.rs` | ADK `AgentTool` for a direct saved child; isolated ADK `SubgraphNode` for a saved-pipeline child; typed descendant-event bridge outside graph state | Ten focused Agent-node/pipeline tests cover strict UI YAML, fixed/variable/f-string mapping, wrong output/no partial state, exact alias/kind/policy and runtime-context lookup, common Runner completion, direct-child breadcrumb streaming, saved-pipeline wrapper and child-LLM hierarchy, namespaced child checkpoints, nested dynamic pause/approve/resume and replay rejection | Partial capability-disabled. Variable-free direct saved agents execute through the exact-version child assembler. Their semantic events stream before node completion under a synthetic pipeline Application call/result, so the UI receives the exact ordered parent tier while graph state remains business/checkpoint state only. An exact saved pipeline compiles at parent assembly into an isolated `SubgraphNode`, shares the claim-fenced Checkpointer, returns only the stable result channel and preserves dynamic child HITL as a parent pause. It emits the ordinary Pipeline wrapper and routes inner LLM events under the exact call-bound `parent_agent_path` while preserving each `langgraph_node`; a private reserved state value only bridges ADK SubgraphNode metadata propagation. The private identity covers both checkpoints and the browser sees neither one. Configured and sensitive-LLM child HITL cards retain the same wrapper hierarchy while preserving the root-bound public interrupt ID; the LLM replay channel reaches the child checkpoint explicitly, so Block With Comment closes the original browser tool call and supplies `sensitive_tool_blocked` under that call ID with zero real-tool dispatch and no fresh planning turn. Recursive Agent nodes inside the selected child remain fail-closed until the ancestry/cycle/hop owner is carried into recursive materialization. Direct-child sensitive continuation, child variables, the private immutable Main resolution gap, runtime-discovered auth refresh, remote effects and inner static interrupts remain gates |
| EliteaUI `ToolkitNode.jsx`, `McpNode.jsx` and `BaseToolNode.jsx`; SDK `runtime/tools/function.py::{FunctionTool.invoke,_build_blocked_termination,_build_mcp_auth_interrupt,_build_mcp_auth_skipped_termination,_build_mcp_auth_refresh_termination}`, `runtime/utils/mcp_oauth.py::McpAuthorizationRequired`, delegated SharePoint/OpenAPI construction and `runtime/langchain/langraph_agent.py::create_graph` Toolkit/MCP branches | Map fixed, state-variable or f-string inputs into one exact configured or MCP action, execute once without a model turn, project declared typed state, pause for sensitive confirmation or delegated authorization, and stop cleanly if either decision declines the call | `src/agents/graph/{direct_tool,compiler,resume}.rs`, `src/agents/{pipeline,runtime,session,events}.rs`, `src/toolkits/{snapshot,materialize,delegated_auth,mcp,invocation,policy}.rs`, `src/toolkits/families/openapi/` | Native ADK `Tool`/`ToolContext`, RMCP HTTP toolset, dynamic OpenAPI ADK tools, family-neutral typed delegated-authorization error, dynamic graph interrupt/checkpoint and `goto END`; Elitea owns exact frozen selection, policy, identity, bounds and projection | Focused direct-node, invocation-wrapper, MCP/OpenAPI materialization, event and pipeline tests cover UI YAML/default mapping, node-family digest, exact call/context identity, dynamic operation schema/RFC query serialization, normal/repaired structured result, masked sensitive pause, MCP challenge redaction, configured-Toolkit cross-origin auth metadata, exact-resource token rebuild, checkpoint approval/authorize/skip, root/pipeline-LLM same-call authorize/skip, sensitive separation, wrong/replayed call rejection, same-ID Toolkit/MCP block/no-call/formatted termination, SDK-compatible null-output terminal state, nested-card aggregation, Main complete-set consumption and effect rejection | Implemented capability-disabled for read-only configured and remote MCP direct actions. Sensitive approval yields the normal result; reject/block emits the correlated structured result and SDK-compatible user explanation. The common delegated-auth signal works for both node kinds while retaining the current public `mcp_auth` contract. Remote-MCP and inline OpenAPI Authorize rebuild the real selected tool with a zeroizing token bound to the frozen resource; Skip nulls declared data outputs, preserves the current formatted explanation and routes to `END`; a still-guarded rebuild fails closed. Materializer-known root and pipeline-LLM calls use native original-call confirmation and exact replay. Same-family nested cards retain exact identity and hierarchy under one terminal pause. Main rehydrates and atomically consumes a bounded exact complete set; independent partial resume remains closed. SharePoint, remote OpenAPI specs, rich OAuth discovery/DCR metadata, runtime 401 re-authorization and non-JSON/artifact bodies remain documented family gaps. Rust omits mutable wrappers, raw exception leakage and synthetic user turns. Fixed and declared-parameter catalogue-backed HTTP definitions are claim-materialized by Main. Remote effects and stdio remain gated |
| Future customer feature; current Python `elitea_sdk/runtime/langchain/langraph_agent.py::create_graph` has no graph-parallel node | Bounded explicit pipeline fork/join for owned Agent nodes, deterministic result order, aggregate nested pauses, and restart-safe child reuse | `docs/parallel-pipeline-node-design.md`, then `src/agents/graph/{compiler,parallel}.rs` and `src/state/postgres_checkpointer/parallel_children.rs` | Custom ADK `Node`; each owned Agent branch is a separately checkpointed child `CompiledGraph`. Elitea owns descendant identity, bounds, deterministic join, and exact decision aggregation | Existing prototype tests cover strict standalone YAML, reverse completion, local concurrency, failure drain, terminal reuse, loop identity, pause rejection, and PostgreSQL child replay. V1 must replace the pause rejection with two-branch HITL/auth/clarification, exact complete-set resume, crash/reclaim, UI, and safe-log proofs. | Proposed redesign; the production compiler remains closed. V1 supports `wait: all`, fail-after-drain, two to sixteen Agent branches, and at most eight active branches. Standard child and parent checkpoints recreate aggregate pauses without an interrupt table. Other branch types, explicit nested parallel nodes, `one`, and `many` remain closed. |
| Future data-driven map/reduce feature; LangGraph exposes dynamic `Send`, but the inspected ADK graph does not | Dispatch one bounded state list through repeated instances of one owned worker, then reduce typed results in input order | `docs/map-reduce-pipeline-node-design.md`, then a compiler-owned map node, approved state reducers, and `src/state/postgres_checkpointer/parallel_children.rs` | Custom ADK `Node`; one item-local child graph per input ordinal. Elitea owns item mapping, bounded admission, deterministic reduce, checkpoint identity, and aggregate interrupts | Required proofs cover eight items at concurrency four, rolling admission, duplicate identity, reverse completion, structured outputs, reducer types, process loss, reclaim, nested HITL, and safe progress | Proposed and capability-disabled. `LoopAgent` is sequential. `ParallelAgent` owns a fixed subagent list and fresh process-local shared state. The map node uses the worker's existing input and output mapping, then folds approved append, sum, or merge reducers by input ordinal. It does not require one saved participant per item. |
| SDK `runtime/tools/llm.py::LLMNode._collect_parallel_application_specs`, `_build_parallel_dispatch_specs`, `_run_parallel_application_calls`; SDK `runtime/tools/application.py` hierarchy metadata; EliteaUI execution hierarchy/grouping helpers | Two or more LLM-selected Application calls, including repeated calls to one participant, execute concurrently; containers and leaves render as stable recursive invocations | `src/agents/{ordinary,session,events,application_tools,direct_hitl}.rs` | Native `LlmAgent` parallel tool dispatch for every complete Application-only root or nested direct-agent tool set; Elitea owns the eight-call bound, typed child-event bridge, native branch isolation, frozen presentation join and ordered `{name,call_id,sibling_ordinal}` hierarchy | Same-participant barrier/provider-order proof, repeated-root wrapper projection, real child-model event ordering, synthetic root -> child -> leaf hierarchy fixture, sequential three-tier resume and four collision-safe parallel leaf confirmations through exact resume/root continuation | Capability-disabled recursive direct slice implemented. Mixed toolsets stay sequential, and a mixed owner with a sensitive descendant application fails at assembly because later non-application effects cannot yet be stopped safely. Provider call ID is durable invocation identity and sibling ordinal is display/order metadata. A bounded typed channel multiplexes child ADK events into the parent before the wrapper result, and recursive projection routes them by exact container invocation and parent call identity. Native descendant branches exclude child events from later root-model history, while the root Runner still persists those events in the same claim-fenced session. Parallel confirmations aggregate durably without another child-session or interrupt table; a complete decision set rebuilds and replays the exact persisted call tree through the admitted three agent tiers before one root continuation. Invocation-scoped replay maps distinguish identical child and leaf call IDs under parallel parents. Missing siblings, broken links and a fourth tier fail closed; internal pause results are suppressed and completed sibling results are retained. The same typed hierarchy and exact complete-set replay now cross a pipeline Agent node through one internal graph checkpoint; partial sibling replay and saved pipelines as direct tools remain gates. This is distinct from the future explicit pipeline parallel node |
| SDK `runtime/tools/application.py::Application.invoke/_run`, `runtime/toolkits/application.py::ApplicationToolkit`, `runtime/toolkits/tools.py::{get_tools,_validate_and_track_application}` and `runtime/clients/client.py::get_app_version_details` | Resolve an exact nested application/version, expose a useful variable/task schema, isolate child state/model/tools, and bound ancestry | `src/agents/application_tools.rs`, `src/agents/{pipeline,assembly,direct_hitl}.rs`, `src/agents/graph/application.rs`, `src/transport/{platform_client,runtime_context}.rs` | ADK-compatible tool over a fresh child `LlmAgent` for direct participants and isolated `SubgraphNode` for a graph-selected pipeline; Elitea owns exact-version authority, typed descendant events, tool name/description, tiers/hops/cycles and checkpoint/HITL binding | Parent-child model/tool E2E proves exact direct resolution, model isolation, child-event ordering, durable nested confirmations and sequential/parallel exact-leaf rejoin through three agent tiers; recursive cycle/tier/project and broken-route fixtures bound admission; Agent-node corpus proves exact saved-pipeline resolution, result isolation and nested dynamic checkpoint resume | Foundation capability-disabled: variable-free direct children recurse through frozen saved agents, source-order descriptions, three agent tiers and 25 raw hops are represented, duplicate exact identities are collapsed, and nested configured/MCP sensitivity is registered on each owning child `LlmAgent`. The custom adapter retains native child execution because stock `AgentTool` buffers semantic child events; it uses a bounded typed bridge instead of tool-progress strings. Descendant events remain one durable root-session transcript under isolated ADK branches, so a second child persistence schema is unnecessary. Recursive rejoin reconstructs every frozen parent call and child conversation from that transcript, then consumes each replay entry by owning invocation plus call ID; identical provider call IDs in separate parallel parents do not collide. A graph Agent node wraps one exact saved direct participant in a synthetic Application container, streams descendants, and binds the full sensitive-leaf set to the pending graph checkpoint; it can also select one exact saved pipeline and preserve its dynamic HITL checkpoint lineage. Current Main lacks the private claim-bound frozen/materialized child route; the public mutable SDK PATCH is never used. Direct/ad-hoc pipelines-as-tools, child variables and runtime-discovered auth refresh, effects and static pauses remain activation gates |
| Centry `utils/agent_execution_common.py::{build_parked_result,build_child_launch_payloads,build_parent_reconcile_payload,apply_parallel_reconcile}` | Durable parent park, child launch, reconcile and terminal suppression | `src/agents/parallel_children.rs`, `src/compat/parallel_reconcile.rs` | ADK child checkpoints; Elitea/Main durable coordination | Cross-process crash/reconcile and multi-HITL tests | Blocked: platform currently rejects parked result end to end |
| SDK `runtime/langchain/langraph_agent.py::LangGraphAgentRunnable.invoke` | Conversation/session restoration plus graph checkpoint selection, continuation, unresolved HITL resurfacing and final result | `src/agents/{session,result}.rs`, `src/agents/graph/{compiler,resume}.rs`, `src/state/{postgres_session,postgres_checkpointer}.rs` | Common ADK `Runner + SessionService + Agent`; graph-backed agents additionally receive `Checkpointer` | In-memory common-Runner proof reconstructs a fresh graph around two sequential pauses and runs a value-producing transform to completion; storage component tests cover the PostgreSQL adapters independently | The initial active-node compiler and exact decision resolver compose through injected ADK session/checkpoint traits. Graph-local frontier and resume state stay in the `Checkpointer`, while the user-visible interrupt event stays in `SessionService`; the public ID is derived and verified from both. Fresh state defaults seed only a new invocation and checkpoint state wins on resume. Production claim-fenced PostgreSQL assembly, restart recovery after a continuation event but before graph advancement, unresolved-interrupt resurfacing outside the latest-event shape, saved-pipeline regeneration and complete output policy for remaining value-producing nodes remain gates. No LangGraph row compatibility is required |
| SDK Runner/agent conversation history and Python worker invocation-local execution memory; Main `execution.ApplicationExecutionService.ClaimExecution` and runtime control receipt | Preserve ordered ADK conversation events and app/user/session state for both direct and graph agents while a current Main claim owns writes | Main `internal/application/execution/claims.go` and `internal/transport/runtimegrpc/control/server.go`; Rust `src/protocol/control.rs`, `src/execution/agent_lease.rs`, `src/agents/{ordinary,session,direct_hitl}.rs`, `src/state/{writer_lease,postgres_session}.rs`; migration `services/elitea-main/migrations/agentstate/0002_agent_sessions.sql` | Native ADK 2.2.0 `SessionService`; Elitea owns tenant/project/capability/definition/thread identity, authenticated claim ordering, supervised lease liveness, durable stale-writer takeover and SQL/resource bounds | Control-receipt boundary/mutation corpus, lease-expiry/renewal/shutdown tests, canonical full-event/state unit corpus, cross-execution grant rejection, injected restore/seed-once history, direct sensitive call/confirmation reload, exact authorized decision resolution, approval/denial restart transcripts, migration contract test and credential-gated isolated PostgreSQL component story | Fresh `adk-session.2.0.0.v1` storage and common Runner injection target the separate `agentstate` database under `elitea_runtime`; legacy LangGraph tables remain in `public` unchanged. Main returns its database-authored claim-start time only on a fresh accepted claim. Runner awaits final-event persistence before yielding it, so a direct sensitive pause is standard session history, not a duplicate interrupt row. Main atomically consumes the exact public decision set; Rust verifies it again against raw session identity. The direct proof records an internal protocol user event because ADK `Runner` has no resume-without-user-event API; the one-shot replay model removes it from provider contents, restores exact call/result pairing and recovers the partial suffix before or after result persistence. Graph interrupt state remains in the graph `Checkpointer`. The Rust lease monitor supplies a read-only liveness guard, and the state adapter checks it before writer locking and again before commit. This is intentionally not a distributed transaction. Main/`elitea-core` remains authoritative for frozen initial history. Production bootstrap still chooses invocation-local storage; restricted pool/role wiring, cleanup-owner coverage, transcript cutover and approved effect execution remain gates |
| Python worker `agents/checkpoint.py::CurrentAgentCheckpointFactory`, `agents/sdk_adapter.py::{_execution_memory,_configure_authorization_checkpoint_resume}` and legacy Centry `utils/agent_execution_common.py::{setup_memory,create_memory_saver,configure_checkpoint_resume}` | Select graph checkpoint storage, scope one graph execution context and configure initial/resume input | `src/protocol/control.rs`, `src/execution/agent_lease.rs`, `src/state/{writer_lease,postgres_checkpointer}.rs`, `src/state/postgres_checkpointer/parallel_children.rs`, migration `services/elitea-main/migrations/agentstate/0001_agent_graph_checkpoints.sql`, planned graph compiler composition | Native ADK 2.2.0 `Checkpointer` with the bundled SQLite backend as behavioral baseline; Elitea owns tenant/project/thread/graph-definition scoping, authenticated claim ordering, supervised lease liveness, durable writer control and typed descendant threads | Control/lease guard corpus; bounded/canonical-serialization/error unit tests plus real PostgreSQL 18 complete-field/nanosecond/arbitrary-number/exact-retry/save-order/wrong-thread/definition-isolation/parallel-child terminal replay/release-race/newer-claim/prune/delete/pool-reuse component story; migration static test | Graph-only storage primitive and typed parallel-child factory target the same separate `agentstate` database as sessions, but a distinct fresh `adk-graph.2.0.0.v1` lineage. It does not own conversation transcript and does not read or mutate LangGraph public tables. One shared bounded state `PgPool` is borrowed by transactions. The same claim-start receipt and live lease guard now fence graph writes without reaching into Main's database; the non-atomic two-database limitation described for sessions also applies. Invocation composition, immutable graph-digest derivation, restricted DB grants, cleanup-owner coverage, Linux/pooler/load proof and Python-lineage cutover remain gates |
| Current agentstate deployment database and Main `cmd/elitea-migrate` | Version and deploy native session/checkpoint storage without coupling it to Main's business database or mutating LangGraph tables | `services/elitea-main/migrations/agentstate/*.sql`, `migrations/embed.go`, `internal/infra/db/migrate::{manifest,runner,ledger}.go`, `cmd/elitea-migrate -agentstate` | Independent checksum ledger plus credential-gated PostgreSQL migration story with legacy-table sentinels; local Centry PostgreSQL 18 run verifies seven native tables, two agentstate ledger rows, four untouched legacy tables and no `centry` schema | Implemented migration owner and explicit agentstate-only CLI. The seven runtime tables are two graph-checkpoint and five normalized session tables; `schema_migrations` is an eighth physical bookkeeping table. No per-HITL table is added because direct pauses are session events and graph pauses are checkpoints. The existing Helm Main-database job is deliberately unchanged; a separately credentialed agentstate migration hook, restricted runtime role and backup/cleanup/retention integration remain deployment gates |
| SDK `runtime/clients/client.py::{_inject_summarization,_inject_context_editing}`, `runtime/middleware/summarization.py::SummarizationMiddleware`, Python worker `AgentExecutionPayload.context_settings`, Main `start.go`/`adhoc.go` and `conversation.meta.context_analytics.last_summarization` projections | Token-triggered summary plus optional old-tool-output editing, while retaining continuation and analytics | `src/agents/context_management.rs`, `src/agents/session.rs`, `src/state/postgres_session.rs`, graph-only `src/state/postgres_checkpointer.rs`; future browser analytics adapter | ADK `EventsCompactionConfig`, `IntraCompactionConfig`, `BaseEventsSummarizer`/`LlmEventSummarizer` and Runner context-compaction strategy are candidate primitives; Elitea owns frozen policy/model authority, safe boundaries, durable selection and projection | Admission per branch (disabled, enabled, unknown key, malformed type, out-of-range) plus behavioral proof that an over-budget transcript is compacted while the preserved tail and pinned system turns survive intact, that an under-budget transcript and a transcript ending inside a tool loop are untouched, and that the default-off path never reaches the Runner seam. Required future corpus covers restart/resume, nested isolation, HITL, skills and analytics | Active for direct and ad-hoc agents. `enabled`/`enable_summarization`/`max_context_tokens`/`preserve_recent_messages`/`preserve_system_messages`/`summary_instructions` drive an ADK Runner `CompactionConfig` built from `LlmEventSummarizer`; `enable_context_editing=true` and a non-null `summary_llm_settings` stay `UnsupportedCapability`, and an unknown key or out-of-range value is `InvalidInput` rather than a clamp. Compaction is in-memory per invocation, so the custom PostgreSQL `SessionService` remains the single durable transcript; graph executions still coordinate frontier state through the custom `Checkpointer`, and pipeline-graph context management is refused because a graph binds one model per node |
| SDK `runtime/tools/skill_tools.py::{LoadSkillTool,loaded_skill_names_from_messages,render_skill_registry_index}` | Progressive disclosure, exact attached-name authorization, history-backed loaded state | `src/agents/skills.rs` | ADK dynamic tool binding/history | Load/dedup/compaction/reload/nested-isolation tests | Planned; current focused Python proof is missing |

### First running-platform checkpoint result

The isolated Main, Redis, PostgreSQL, model facade and Rust worker completed a
real ad-hoc chat on 2026-08-25. Execution
`bc39746696860d52761e5a2470e83c47` streamed nine node events, echoed the prompt
through the mock model and persisted the reply. The worker retired the Redis
delivery and remained running with `restart_count=0` after OTLP flush.

The collector received `service.name=elitea-worker-rust` and correlated spans
for command verification, delivery, preparation, native lifecycle, one model
request and five session writes. This ad-hoc execution did not use a graph, so
it correctly emitted no graph-checkpoint write span. The proof also found and
fixed a release-aborting OTLP exporter panic by using Tokio-backed batching.

The same deployment exercised the UI model-configuration path through Main and
Bifrost. The model catalog uses `configuration.data.name`, not the
configuration title, as the runtime model identifier. After the isolated
gateway allowlist admitted `api.openai.com:443`, execution
`c9b27286118d3e3f999297d97f4f94bd` selected `gpt-4o-mini`, did not call the
mock server, reached OpenAI, and received `model_gateway.unauthorized`. The
available shell value is therefore not a usable OpenAI API key. Rust published
the durable `execution.failed` terminal, retired the delivery and stayed at
`restart_count=0`. The repository smoke reader now recognizes that terminal by
its SSE `event:` name; runtime failures do not carry the node-event
`data.type`.

A credentialed follow-up then exercised the Rust `ModelFacade` through the
current local Elitea OpenAI-compatible proxy. The host-facing provider base was
`http://localhost:80/llm/v1`; the isolated gateway used Docker's stable host
alias for the container hop. Execution `868af70730dc53444aeb27b7343f0f51`
selected `eu.anthropic.claude-sonnet-4-6` with
`openai_compatible=true`, streamed a unique response marker, persisted the
assistant reply and retired normally. Both worker and gateway stayed at
`restart_count=0`.

The proof used the existing `open_ai` credential type, not a new user-facing
provider. The gateway keeps official `api.openai.com` bases on Bifrost's native
OpenAI provider. It maps a linked `open_ai` credential with another `api_base`
to Bifrost's per-key compatible provider, while reading no key material in the
model resolver. Bifrost appends `/v1/chat/completions`, so gateway account
normalization removes exactly one trailing `/v1`; both `/llm` and `/llm/v1`
stored values are operational.

The next rehearsal used an OpenAPI credential and one selected dynamic
operation. Execution `f25b5e2b95e68575c5656dd9fa577a43` exposed only
`echo_marker` to the root `LlmAgent`. Rust dispatched the exact marker to
Postman Echo, correlated the result to the original provider call ID, ran the
second model turn and reached EOS. Reloaded history contained exactly
`RUST_OPENAPI_TOOL_E2E_20260828_B`. The worker retired the delivery normally.

The same Private-project browser rehearsal then proved live tool-result
regeneration rather than only a second ordinary turn. The initiating request
retained its exact `question_id` while Rust/Main replay frames carried
`question_id: null`; the UI now fills only that missing live-frame field from
the admitted request and never overwrites an explicit event identity. The
versioned `agent.regenerate.v1` request returned 200 for execution
`6005d8b12b90e47ac1c24ad9335d8544`, invoked `echo_marker` again under a fresh
provider call ID, completed the second model turn and survived a full-page
reload with `RUST_REGEN_TOOL_E2E_20260828_G` as the final reply. Main history
projection also omits a failed user/assistant pair together, so a later
regeneration cannot inherit an orphan prompt whose partial assistant response
ended in an error.

This UI fallback does not claim a complete replay contract. The current
`AgentExecutionCommandV1` carries the response-message and stream identities
but not the question identity. `execution_generation` cannot substitute for
it: an initial execution uses the question UUID, while regeneration uses a new
regeneration UUID. A late-attaching client that has only durable node events
therefore still cannot recover the question edge until Main's persisted chat
history is available. Closing that gap requires an explicit optional
language-neutral question identity rather than inference. Reload also restores
the final assistant text but not the completed live tool-action accordion; that
is a separate UI history-projection gap and does not weaken the recorded fresh
tool invocation proof.

The current UI participant behavior is the compatibility reference. Toolkit
and MCP choices are checked switches that remain open during add or remove.
Agents and pipelines are catalog selections that close the menu and activate
the selected participant. The replatform UI now proves existing-chat toolkit
remove and add through one 204 DELETE and one 200 POST. New-chat staging and
default-model assignment for model-free saved participants remain open UI
gates. The replatform OpenAPI form also lacks the current generic additional
header editor.

The next platform proofs cover one frozen saved child, one real graph
pause/resume with PostgreSQL checkpoint inspection, and the native
OpenAI/Anthropic tool-calling matrix. Approved effects, Kubernetes activation
and load tests remain later gates. Indexing and RAG remain last.

## MCP, HITL, budgets, and cancellation

| Python source path and symbol | Observable behavior | Rust target | Native ADK primitive or Elitea ownership | Proving tests | Status / deviation |
| --- | --- | --- | --- | --- | --- |
| SDK `runtime/toolkits/mcp.py::McpToolkit` and direct authorization handling in `runtime/tools/function.py`; Centry OAuth/DCR, catalog/sync and MCP-exposure sources enumerated in the MCP capability split below | Remote MCP discovery, selected tools, sessions, exact-server token continuation and invocation | `src/toolkits/{mcp,invocation,delegated_auth}.rs`, `src/agents/{ordinary,pipeline,runtime,session,events}.rs`, `src/agents/graph/{direct_tool,llm,resume}.rs`, `src/execution/{native_agent_lifecycle,output_delivery}.rs` | RMCP HTTP client/toolset plus Elitea admission, guarded selected-tool placeholders, native original-call confirmation, graph interrupt and one claim-bound pause aggregator | HTTP MCP discovery/invoke/timeout/error-bound tests plus fixed prebuilt header, exclusion, challenge, token, and catalogue rebuild, root and pipeline-LLM pause/authorize/skip/same-ID replay, sensitive-after-auth separation, direct-MCP-node exact-checkpoint fixtures and ordered multi-card terminal aggregation | Partial capability-disabled: exact selected plus fixed and declared-parameter catalogue-backed HTTP reads, with materializer-known on-demand authorization in direct agents, LLM nodes and direct MCP graph nodes. Nested same-family cards retain exact hierarchy as immediate progress and one `PausedMcpAuth` terminal aggregate. Main-origin internal PAT stamping is implemented at claim materialization. Independent partial resume, mixed-guardrail aggregation, platform OAuth/DCR callbacks, descriptor sync, stdio, effects and Elitea-as-MCP-server exposure remain separate gates |

ADK 2.2.0 preserves HTTP authorization status/headers while establishing the
RMCP transport, which is the challenge handled by this slice. Its private
`McpTool` converts a later `tools/call` transport failure to an `AdkError::tool`
string, losing the typed status and challenge headers. Rust therefore does not
parse that text: post-discovery token expiry remains an explicit ADK integration
gap until the call boundary can expose typed transport metadata.
| SDK `runtime/toolkits/mcp_config.py::McpConfigToolkit` | Saved MCP definitions, static/live discovery, HTTP or stdio | `src/toolkits/mcp.rs`, Main prebuilt catalogue and claim materializer, external MCP runner client | ADK remote MCP only | Fixed HTTP header, exclusion, token-precedence, authority, parameterized schema, and live catalogue tests | Partial capability-disabled: fixed and parameterized HTTP definitions run after Main claim-time resolution. PostgreSQL catalogue changes apply to later claims. Arbitrary stdio never runs in the main worker |
| SDK `runtime/utils/mcp_oauth.py::McpAuthorizationRequired`, configured delegated-auth producers such as `configurations/{sharepoint,openapi}.py`, and Centry MCP auth helpers | One compatibility-named authorization signal across MCP and general Toolkits; direct-node pause versus deferred LLM authorization; public pause shape | `src/toolkits/{delegated_auth,materialize}.rs`, `src/toolkits/families/openapi/`, `src/agents/graph/{direct_tool,llm,resume}.rs`, `src/agents/{direct_hitl,events,ordinary,pipeline,runtime,session}.rs`, `src/execution/{native_agent_lifecycle,output_delivery}.rs` | Native ADK confirmation/session replay for model calls and dynamic graph interrupt/checkpoint for direct nodes; Elitea retains family-neutral `mcp_auth` routing, token ownership and UI | Typed-signal preservation/redaction, MCP and configured-Toolkit direct pause/skip/authorize seam, root/pipeline-LLM original-call authorize/skip, sensitive-after-auth separation, cross-origin OAuth metadata, OpenAPI exact-base-URL rebuild, exact checkpoint tests, ordered nested-card terminal aggregation and Main complete-set consumption | Partial capability-disabled: the family-neutral control path and public exact-ID/hierarchy aggregation are implemented; remote MCP and bounded inline OpenAPI perform concrete token rebuilds in direct and model-owned calls. Independent partial sibling resume, mixed-guardrail aggregation, runtime-discovered challenges, SharePoint and remaining OpenAPI OAuth/content capabilities remain gates |
| SDK `runtime/tools/hitl.py::HITLNode` | Pipeline HITL routes and edit/block behavior | `src/agents/graph/{hitl,agent,compiler,resume}.rs`, `src/agents/events.rs` | ADK `interrupt_with_data`, graph checkpoint/resume and `goto`; Elitea owns definition/decision identity | Strict/default/bound template fixtures, real graph checkpoint/resume, edit/block/fail-closed action, browser projection and complete-document sequential-resume tests | Core node plus initial capability-disabled compiler/resume path implemented. It supports fixed/variable/bounded-fstring messages, approve/reject/edit routes, one-use private decision consumption and checkpoint-bound public identity. A Main-authorized decision must match the latest durable interrupt event and exact checkpoint/thread/pending node/definition digest; stale or already-advanced state fails closed. Fenced PostgreSQL production composition, continuation-suffix crash recovery, static before/after breakpoints and parallel interrupt aggregation remain gates |
| SDK `runtime/middleware/sensitive_tool_guard.py::SensitiveToolGuardMiddleware`, `runtime/tools/function.py::FunctionTool._build_blocked_termination`; Main `agentexecution/continue.go::ContinueCurrentAgent` and SQL `ResumeCurrentAgentHITL` | Policy matching, masking, approve/reject/block-with-comment, fresh invocation identity, atomic browser-decision consumption and clean direct-node termination | `src/agents/{application_tools,sensitive_tools,direct_hitl,session,events}.rs`, `src/agents/graph/{llm,node_events,direct_tool,resume}.rs`, `src/toolkits/policy.rs` | ADK `require_tool_confirmation` for LLM calls plus `interrupt_with_data`/checkpoint/`goto END` for direct graph nodes; Main owns public-card consumption | Runtime/admin matching, direct and pipeline-node LLM-tool pause/replay/block, graph Toolkit/MCP pause/no-call/exact resume/replay, masked projection, structured same-ID block, node-labelled native lifecycle events, formatted terminal explanation, saved-pipeline child-card hierarchy and same-ID LLM replay, typed-state preservation and four parallel nested confirmations with complete three-tier exact-leaf resume | Initial capability-disabled direct and graph confirmation paths are implemented. LLM-owned calls use native confirmation: direct agents persist the exact turn in session history, while a pipeline LLM node binds its exact pending ADK content and provider call ID to the existing graph-interrupt/session persistence and validates it against the graph checkpoint. Approval returns the ordinary tool result. An LLM denial runs the common no-effect adapter and continues the same native tool loop with `sensitive_tool_blocked` under the original call ID; it adds no user turn and performs no fresh planning call. Materializer-known remote-MCP authorization uses the same original-call machinery: Authorize rematerializes exactly without consuming a separate sensitive approval, and Skip returns `mcp_auth_decision` without dispatch. Pipeline LLM model/tool lifecycle events are forwarded outside graph state with their owning node identity; a denial closes the original browser tool run before the final model turn. Direct Toolkit/MCP nodes have no model loop, so they use one graph interrupt before dispatch. Their public identity joins invocation, private checkpoint thread/checkpoint, pending node, definition, visit-specific call ID and canonical arguments. Approval returns the ordinary result. Reject/Block With Comment executes zero provider calls, records the same structured result, preserves the SDK's user-facing markdown explanation as a distinct assistant message and routes to `END`; Rust deliberately avoids Python's incompatible null writes to declared typed outputs. Nested direct-agent confirmations are branch-isolated, persisted in the root session and projected as distinct recursive UI cards without an extra interrupt table. Main consumes the bounded decision set atomically; Rust rejects incomplete sets, reconstructs the complete direct-agent replay tree by exact invocation/call identity, projects native confirmation-tagged results at each tier and continues the root once. A pipeline Agent node converts the complete descendant interrupt-ID set into one internal graph checkpoint while leaving descendant cards as the only public HITL events; approve and same-call structured block replay the exact hierarchy, and incomplete sets fail before dispatch. Same-family nested authorization cards now aggregate under one exact terminal pause; approved-effect receipts, incremental tool-progress chunks, Main plural/partial authorization activation and mixed-guardrail terminal aggregation remain closed |
| SDK `runtime/exceptions.py::{BudgetExceededError,budget_exceeded_from}` | Project/member budget exhaustion is terminal and not recoverable tool content | `src/agents/budget.rs`, `src/compat/current_agent_result.rs` | ADK/Tokio cancellation; Elitea classification/message | Sibling cancellation and no-normal-success tests | Planned |
| Centry `module.py::TaskNode(kill_on_stop=True)` and Python worker `execution/delivery.py::_ClaimLeaseMonitor` | Durable Stop remains distinct from worker drain; renewal continues after Stop and a later fatal lease loss remains authoritative | `src/agents/runtime.rs::NativeAgentRun`, `src/execution/agent_lease.rs::ClaimLeaseStateProbe`, `src/execution/native_agent_lifecycle.rs` | Elitea read-only lease probe plus an invocation-exclusive ADK `Runner`; ADK's session-wide interrupt APIs are never exposed | `src/agents/runtime_tests.rs::stop_is_scoped_to_one_same_session_native_invocation`, `src/execution/agent_lease_tests.rs::a_fatal_failure_after_cancellation_becomes_authoritative`, `src/execution/output_delivery_tests.rs::durable_stop_interrupts_only_the_owned_run_then_settles_cancelled`; broader tool-effect fencing remains planned | Implemented inside the capability-disabled ordinary-text lifecycle. One native run can be cooperatively stopped without cancelling a same-session sibling; the lifecycle continues renewal, drains the stopped Runner to EOS, and a later fatal lease transition still suppresses output. Worker supervisor drain never calls business Stop. Exact ADK-Rust 2.2.0 has no public per-run cancellation handle, so each authorized invocation owns one non-shared Runner and only the durable lease state can call its private interruption boundary |

Pipeline-Agent authorization addendum: the saved direct-agent participant path
now accepts the same bounded exact `mcp_auth` decision list already used by
direct saved-agent execution. Its existing internal graph interrupt binds the
complete descendant ID set to the pending Agent node and checkpoint. Parallel
Authorize/Skip and a mixed sensitive-plus-auth set retain their public
hierarchies, replay the same original calls without planning again, and reject
partial or mismatched authority before dispatch. This supersedes older cells
in this file that list pipeline-Agent or broad parallel/nested authorization as
an open generic-MCP gate.

### Checkpoint-bound delegated authorization mapping

| Current Python behavior | Rust ownership | Preserved behavior and proof |
| --- | --- | --- |
| SDK `runtime/middleware/sensitive_tool_guard.py`, `runtime/tools/function.py::{_build_mcp_auth_interrupt,invoke}` | `src/agents/{direct_hitl,session,events}.rs`, `src/toolkits/delegated_auth.rs` | Treat `mcp_auth` as an ordinary native confirmation tagged with sanitized auth configuration. The deterministic interrupt and original tool-call IDs are resolved against the persisted session event; Authorize and Skip are rejected for a sensitive card, while approve/reject are rejected for an auth card. |
| SDK `runtime/tools/llm.py` materialization and `parallel_mcp_auth` aggregation | `src/agents/{ordinary,application_tools,direct_hitl,session}.rs`, `src/toolkits/mcp.rs` | Retain an auth catalog on every owning root or nested `LlmAgent`, use native `require_tool_confirmation`, and resolve a bounded complete decision set. Exact-server token and declined-server authority must equal the servers named by the persisted decisions; unused or contradictory authority fails closed. |
| SDK `runtime/tools/application.py` deferred child interrupts and exact child resume | `src/agents/{application_tools,events,session,pipeline}.rs`, `src/agents/graph/resume.rs` | Persist child confirmations in the root ADK session, retain `{name,call_id,sibling_ordinal}` hierarchy metadata, rebuild the same frozen child tree and consume replay plans by container invocation plus call ID. Parallel Authorize executes each original call once; Skip returns `mcp_auth_decision` under each original call ID and dispatches no real tool. The same replay tree now crosses a pipeline Agent node through its existing graph checkpoint; partial sets fail before materialization, and mixed sensitive/auth leaves resume atomically. |
| SDK `runtime/langchain/langraph_agent.py` auth decision extraction/checkpoint hydration; legacy Core `rpc/chat_all.py` parallel child ownership; Main `agentexecution/continue.go`, `repos/agent_start.go`, `agent_chat.sql`; standalone Python worker `agents/sdk_adapter.py` | `src/agents/{runtime,direct_hitl,session,pipeline}.rs`, `src/agents/graph/resume.rs`; Main scalar and bounded complete-set admission | Rust accepts the exact normalized set through the existing versioned `hitl_decisions` JSON field with `guardrail_type=mcp_auth` and `action=authorize|skip`. Pipeline admission enables token/decline authority only when the parsed exact decision set contains an authorization action; the later persisted-card join rejects unused or mismatched authority. Main maps its exact scalar authorization route into one such decision and also accepts a bounded exact complete set, preferring durable `interrupt_id` over legacy `tool_run_id`, restoring each persisted `tool_call_id`, sorting for stable idempotency and atomically consuming all siblings. Incomplete sets do not resume the worker. The standalone Python worker admits the same shared wire and forwards it unchanged to the current SDK. No protobuf field or interrupt table was added. |

The remaining MCP/on-demand-auth gates are independent partial sibling resume,
mixed-guardrail terminal aggregation,
post-token model-loop challenges, remaining SharePoint content/app-only tools,
remaining OpenAPI capabilities, platform
OAuth/DCR and production activation. Materializer-known root, pipeline-LLM and
parallel nested calls, including a pipeline Agent owner, are covered.

The worker delivery lifecycle now keeps every sanitized pause card browser-visible
as ordinary progress, then mints terminal authority only from one bounded ordered
aggregate after ADK EOS. Authorization aggregates preserve `interrupt_id`,
`tool_call_id`, toolkit family and recursive UI hierarchy and settle as
`PausedMcpAuth`; HITL aggregates use the same owner and settle as `PausedHitl`.
Mixed guardrail families remain explicit rather than being silently collapsed.
`ProjectedAgentEventBatch` is a heap-owned bounded vector: the previous 35-slot
inline array was 10.9 KiB and nested async projection could overflow the normal
2 MiB runtime/test stack. The hard 35-event cap is unchanged.

This checkpoint supersedes older broad-row phrases that list all
"parallel/nested authorization" as closed: the implemented scope is the
materializer-known direct `LlmAgent`/saved-agent hierarchy and that hierarchy
inside a pipeline Agent node, plus Main's bounded exact complete-set admission.
Older `Main plural/partial activation` wording now means independent partial
resume only. It does not claim mixed-guardrail terminal aggregation,
post-token model-loop challenges, remaining SharePoint content/app-only tools
or the remaining OpenAPI capabilities.

SharePoint checkpoint update: the explicitly selected delegated Graph read core
is now a concrete configured family in
`src/toolkits/families/sharepoint/{config,client,tools}.rs`. It materializes
eight schema-complete read tools, preserves composite/discovery/site token-key
precedence, and joins the same exact-resource authorization catalog used by
root, nested and pipeline `LlmAgent` owners. Direct Toolkit nodes also convert
a real Graph 401 into the common native graph interrupt. Empty/all selection,
ACS/app-only auth, document and attachment parsing, artifact operations, effects
and a post-token 401 adapter for an already-running model loop remain closed.
This paragraph supersedes older broad table cells that call all SharePoint
materialization unported.

## Known gates

- The restricted Redis retirement adapter now consumes opaque terminal proof
  inside the same verified command, full execution identity, outbox ID and
  exact signed-envelope binding. The real restricted redis-rs/Rustls client and
  Lua command surface are implemented; a TLS/ACL Redis 7 service/component test
  still must prove them against deployment policy before capability admission.
- Centry now consumes `truncated_content`, but `AgentExecutionInputV1` has no
  corresponding field. This remains a contract-owner decision, not a Rust-only
  extension.
- `PARKED_CHILDREN` exists in the proto enum, but the committed Python/Go path
  rejects parallel reconcile inputs and the terminal state. The Rust runtime
  must not advertise this capability until Main, worker delivery and replay are
  proven together.
- Nested-skill freezing currently exists only as uncommitted work in the
  separate Go/Python thread. It is candidate evidence, not the committed Rust
  baseline.
