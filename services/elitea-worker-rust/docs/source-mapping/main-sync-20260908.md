# Main integration checkpoint: 2026-09-08

The integration starts from Rust branch `046e84a2` and incoming main `0286138a`.
It preserves both histories. It does not reset the branch to an older commit.

Current status: the branch includes main `007c597f` and deploys source `0f789b57` across all four rehearsal services.
The active database copy has canonical shared migrations through 123.
The original databases remain unchanged.
See the completed cutover and browser proof below. Earlier sections retain their checkpoint history.

## Contract reconciliation

Main and the Rust branch independently assigned the same toolkit protocol numbers.
The integration preserves main's published numbers and moves the unmerged Rust read contract.

| Contract | Command enum | Command field | Output enum | Output field |
| --- | --- | --- | --- | --- |
| `toolkit.call_tool.v1` | 10 | 36 | 7 | 25 |
| `toolkit.execute.read.v1` | 11 | 64 | 8 | 33 |

Reserved field ranges remain reserved. Generated Go and Python bindings come
from the merged schemas. Rust's wire scanner recognizes both command fields.
It rejects multiple command alternatives before normal decoding.

The new Test contract remains a required Rust gap. See
[`toolkit-test.md`](toolkit-test.md), gate `TKTEST-RUST-01`.

## Application integration

- Keep main's session-authenticated API composition and toolkit Test service.
- Keep Rust read execution behind explicit Rust worker selection.
- Keep internal MCP executors and main's current toolkit catalogue.
- Use main's tenant-scoped application-skills reader for internal MCP skill listing.
- Keep skill instructions out of the public application-skills JSON response.
- Keep main's pipeline configuration panel and restore the MCP exposure toggle there.
- Keep main's MCP Load Tools composition and the existing OAuth flow.
- Use explicit MCP discovery results before older catalogue tool names.

## Migration and deployment gate

Main already owns shared migrations 111 through 120.
The branch's prebuilt-schema and read-execution migrations become 121 and 122.
Migration 122 admits both toolkit contracts in shared database constraints.
Previously published main migrations remain unchanged.

The rehearsal ledger was inspected without writes on 2026-09-08.
It still records branch migration 111 as `mcp_prebuilt_parameter_schema` and
112 as `toolkit_execute_read`. These names conflict with main's ledger.

Do not deploy this merge directly over that ledger.
Do not change checksums to conceal a migration conflict.
Do not replay pending old read-command bytes as the new Test contract.

Prepare a separate cutover procedure before deployment:

1. Preserve a recoverable database and runtime-output snapshot.
2. Inventory old read executions, prepared commands, pending deliveries, and output spools.
3. Finish or explicitly retire old work under its original protocol.
4. Rehearse migration reconciliation on an isolated database copy.
5. Preserve existing execution rows and both toolkit constraints during reconciliation.
6. Verify the final ledger, schema, chat history, and runtime recovery before service replacement.

Main migration 115 changes capability constraints. Old Rust read rows need
explicit handling before that migration runs. Renumbering files alone is not a
complete rehearsal-database upgrade.

### Copy-first rehearsal evidence: 2026-09-08

Private custom-format dumps preserve the product database and the separate agent-state database.
Their archive listings pass validation. A separate copy preserves the encrypted worker output spool.
The product dump restores successfully into an isolated database.
These snapshots come from live services. They are not a quiescent, cross-database-consistent cutover backup.

The current migrator refuses the isolated copy at shared migration 111.
Its error identifies `0111_default_project_roles_and_bootstrap_account.sql` as a name mismatch.
The complete copied database data digest is unchanged before and after that attempt.
This proves refusal without data changes. It does not prove a successful upgrade.

The read-only inventory script is
[`scripts/database/rust-main-sync-preflight.sql`](../../../../scripts/database/rust-main-sync-preflight.sql).
It applies statement and lock timeouts. It prints no signed commands, input payloads, results, or fence tokens.
Its successful execution means inspection succeeded, not that deployment is safe.

The copy contains one unsettled old-format direct-read command and one saved terminal output.
The output authenticates under its original protocol and matches claim 1.
The copied database has already reached claim 1,756.
Repeated new-fence rejection prevents replay and settlement before the deadline branch can run.
See [toolkit-terminal-recovery.md](toolkit-terminal-recovery.md) for the cause and tested current-protocol correction.

Do not decode that saved frame with the merged output field assignments.
Do not rewrite its signed command or hide the migration mismatch.
Complete original-protocol recovery before the schema cutover.

No live migration, claim rewrite, command retirement, or service replacement occurs in this rehearsal step.
Existing chats, authorization state, application rows, and the original output remain intact.

### Original-protocol recovery: 2026-09-08

The isolated worker repair at `4a422f5d` settles the live old-format direct-read execution.
Its signed deadline has expired. Main commits a deadline failure under replacement claim 1,815.
The normal runtime path removes the exact Redis delivery and acknowledged spool frame.
Read-only checks confirm no further reclaim for this execution.
See [toolkit-terminal-recovery.md](toolkit-terminal-recovery.md) for the image, timestamps, and verification record.

This service replacement changes only the old-protocol worker.
It preserves the previous configuration, mounts, security settings, and resource limits.
It does not deploy the merged Main, UI, or worker protocol.
It does not alter the migration ledger or manually retire a command.
The original snapshots remain available. The isolated database copy retains its earlier historical state.

The existing UI completes a new chat turn and retains its answer after reload.
This is a compatibility smoke test, not the required merged-UI authorization proof.

Gate `REC-RUST-01` is complete. Database reconciliation remains open.
Repeat the pending-work inventory and take a quiescent backup before the actual cutover.
Do not infer that settling one known execution makes the full rehearsal database ready for migration.

## Remaining order

### Latest main refresh: `007c597f`

The PR branch remains `feat/rust-worker-continuation`.
The latest merge preserves main's new UI, API, gateway, and test changes.
The temporary old-protocol repair branch is operational provenance, not a second PR branch.

Main now owns migration 121, `bulk_invite_permissions`.
The unapplied prebuilt-schema migration moves from 121 to 123 without SQL changes.
The read-execution migration remains 122.
The earlier numbering above records the previous integration checkpoint.
At this merge checkpoint, the running rehearsal ledger still contains the original branch migrations 111 and 112.

The UI merge retains token-aware authorization and exact interrupt routing.
Main's new MCP authorization factory calls the existing authorization implementation.
It does not send an empty token or use the old socket fallback.
Tests cover parallel decisions and tool-run identity fallback.

The graph merge retains main's new second-question regression test.
It uses the existing `TurnCheckpointer` to isolate fresh input without deleting previous execution checkpoints.
An initial merged test detects checkpoint deletion. The corrected merge passes that test and the full Rust suite.

Verification after this refresh:

- Generate the Go OpenAPI bindings from the merged schema.
- Main package compilation passes. Focused migration, configuration, and execution tests pass.
- MCP registry tests pass against isolated rehearsal databases. No tests skip.
- Rust passes 905 library tests and 84 integration/contract tests. No tests are ignored.
- The selected UI suite passes 250 tests across 17 files. UI type checking passes.
- The separate gateway module passes `GOWORK=off go test ./...`.

These checks do not prove deployment compatibility or OAuth browser completion.
The database-copy reconciliation and merged service deployment remain open.

### Successful copy reconciliation: 2026-09-08

The copy-only [operator procedure](../../../../scripts/database/rust-main-sync-rehearsal.md) resolves the known collision without changing the ordinary migrator.
It archives both old receipts and executes canonical migrations 111 through 123 unchanged.
It restores all 14 staged runtime tables and compares every row before commit.
The procedure uses a single transaction and rejects normal database names.

All five unit/component tests pass with the isolated fixture. No test skips.
Tests cover logical-row rollback, injected precommit failure, unknown dependencies, complete restoration, and repeat refusal.
The unchanged current migrator accepts the reconciled copy twice, including tenant histories.
No original receipt is replaced with a false checksum.

Only the isolated copy changes in this proof.
The original database, separate agent-state database, Redis delivery state, and running services remain unchanged.
A fresh quiescent backup and pending-work inspection remain required before the actual cutover.

### Completed rehearsal cutover: 2026-09-08

The collision is a branch migration-number conflict, not evidence of database corruption.
Main owns canonical migrations 111 and 112. The Rust migrations now use 123 and 122.
The normal migrator correctly refuses the conflicting old ledger.

The cutover stops only rehearsal Main, web, gateway, and Rust worker.
Fresh product and agent-state dumps follow service shutdown and the absence of other database clients.
Separate copies retain the worker material and output spool.
The cutover creates new product and agent-state databases from these dumps.
It changes only the copied worker connection material to select the copied agent-state database.

The copy procedure restores all 14 runtime tables and validates every staged row.
The restored rows include 1,051 executions, 16,342 claims, seven read jobs, and one read result.
The archive retains both original migration receipts.
The unchanged migrator accepts the canonical shared ledger, tenant histories, and agent-state schema before service replacement.

The original product database still records branch migrations 111 and 112.
The active product copy records canonical migrations through 123.
The original product and agent-state databases, worker volumes, images, and private deployment snapshots remain available.
The cutover does not delete chats, authorization state, historical jobs, or application rows.

Redis, PostgreSQL, the emulator, and the separate legacy platform remain running.
This proof does not include a cold Redis restore or a production rollback exercise.
Historical expired jobs remain unchanged. They are not manually cancelled or deleted.

The replacement uses the documented Rust selection and Main self-call defaults:

- Set `ELITEA_WORKER_IMPLEMENTATION=rust`.
- Leave `ELITEA_RUNTIME_CURRENT_MAIN_BASE_URL` unset for the local cleartext Main listener.

The old deployment supplied an HTTPS self-call URL for that cleartext listener.
Current Main rejects that mismatch. Removing the obsolete override permits normal startup.
See `deploy/docker-compose.standalone-full.yml` and `services/elitea-main/internal/runtimecomposition/config.go`.
This correction changes deployment configuration, not product code.

All four images contain source `0f789b57`:

| Service | Image ID |
| --- | --- |
| Main | `sha256:f89b6a8d47af4e5f9946847cff8a884ba5faa1019aed9e32a20e0645c3f6e9ad` |
| Web | `sha256:537de223f146cf0753fcb6b32810728cdee80c32ea3f67a899d422c657c827ea` |
| Gateway | `sha256:ffa10f72f50e4341a55961f56b512ef82ac5f89a7f739dd98100a90e03ad9274` |
| Rust worker | `sha256:9f102b9947a9ed09559e73db6ca12147df9607444234b6ca2659c58942234103` |

Main and web report healthy. Gateway health and readiness return HTTP 200 over verified mutual TLS.
All four containers remain running with zero restarts at the final check.
The retained worker container label names the earlier repair deployment; the worker image label identifies source `0f789b57`.
Image identity, not that historical container label, establishes the deployed worker revision.

### Browser proof on the merged deployment

Playwright uses the new UI in the Private project. Existing user tabs remain intact.

- Conversation 538 retains its old answer and completes `RUST_MAIN_SYNC_20260908` as a new ordinary turn.
- Reload preserves both answers and the selected model.
- Conversation 536 completes public DCR registration, browser consent, code exchange, durable resume, and an actual Rust MCP call.
- The first result contains `RUST_MAIN_SYNC_DCR_PUBLIC_20260908`, mode `dcr-public`, and generation 1.
- After document reload and access-token expiry, a new turn returns `RUST_MAIN_SYNC_DCR_REFRESH_20260908` with generation 2.
- Regenerate executes the tool again and returns the same marker with generation 3.
- Reload preserves the regenerated answer. No new consent prompt appears during refresh or regeneration.

The emulator's public DCR registration, consent, and code-exchange counters remain at 5, 4, and 4 after the initial grant.
Its refresh counter advances from 1 to 3. Its successful tool-call counter advances from 2 to 5.
These counter changes distinguish actual refresh and execution from a model repeating an old answer.

Persisted message and generation identifiers correlate refresh to execution `106e6915d81602e35485981440904451`.
Regeneration correlates to execution `3f462b9ac7a1b9830e329a0c262ba6c1`.
Both executions succeed on claim 1, release their claims, and commit settlement.
The initial authorization path uses executions `5d6a4f078766f2c2db43adc541367103` and `20dcb27c4ebfef60e46b6340814eabb5`.
The final tool result proves completion; an outer authorization-pause settlement alone would not.

The browser also exposes a separate participant-panel wiring gap.
The conversation API retains MCP toolkit 28, and the worker executes it successfully.
Both MCP visibility settings are true, but the panel omits the MCP.
`apps/elitea-web/src/pages/chat/index.tsx` does not pass `isMcpVisible` to `ParticipantsWrapper`.
`features/chat-participants/ui/Participants.tsx` defaults that flag to false and filters the MCP group.
Track this UI integration gap separately from runtime attachment and authorization correctness.

### Next slices

1. Close the participant visibility wiring gap with a mounted-page regression test and browser proof.
2. Resume OAuth/DCR lifecycle gaps from `delegated-oauth-dcr.md`, including confidential-client reload ownership and concurrent-tab logout.
3. Implement `TKTEST-RUST-01` with its security and replay gates.
4. Continue the other source-mapped runtime gates.

This checkpoint does not close live-provider, browser, load, or Kubernetes gates.
Detailed runtime errors remain tracked under `OBS-RUST-01`.

## Initial integration verification record

- Protocol, sqlc, and OpenAPI generation completed.
- Main package compilation passed with `go test -p 4 ./... -run '^$'`.
- The selected Main suite passed across 15 packages: 2,848 test/subtest results passed; 252 skipped.
- The skipped Main tests require separate database or service fixtures. They do not prove those contracts.
- The focused Main race suite passed across 11 packages.
- Main MCP catalogue tests passed against isolated rehearsal databases: 53 test/subtest results; no skips.
- Rust passed 890 library tests and 84 integration/contract tests. No tests were ignored.
- Rust PostgreSQL session and checkpoint component stories ran against isolated rehearsal databases.
- Rust formatting and Clippy checks passed with all targets and features.
- Web type checking passed. The selected UI suite passed 639 tests across 57 files.
- Python's new toolkit-call handler suite passed 20 tests against the regenerated protocol bindings.

The Python image capability snapshot did not pass in the shared local environment.
Its SDK lacks the required `FAILED_IMPORTS` registry attribute; seven snapshot tests fail there.
Repeat that suite in the lock-pinned worker image. Do not change the snapshot to match the older local SDK.

No merged service image was deployed at this initial checkpoint.
No existing rehearsal migration ledger, chat, authorization token, or application row was changed at that point.
The tests removed only their own isolated databases and temporary runtime test state.
