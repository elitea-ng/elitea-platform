# Main sync: 2026-09-09

## Revision boundary

The continuation branch starts this sync at `e2eb756a`.
The fetched main head is `f28189b5`.
Incoming commits are `7244490c` and `f28189b5`.
The sync preserves one continuation branch and uses a merge, not a reset or history rewrite.

Main adds UI parity, E2E coverage, model grants, application timestamps, and shared vault write locking.
Rust source and protobuf inputs do not change in these incoming commits.
Existing generated protobuf bindings therefore need no regeneration for this sync.

## Conflict resolutions

| Boundary | Combined behavior |
| --- | --- |
| Chat page imports | Preserve saved-agent editing, participant addition, and the new canvas callbacks. |
| Application creation | Persist tags once and return the stored set. Preserve failure cleanup. |
| Version deletion | Delete skill bindings and update the owning application timestamp in one tenant-local transaction. |
| Version persistence | Preserve skill-copy selection and pipeline settings alongside Main's metadata merge and timestamp updates. |
| OpenAPI | Preserve author metadata, version skill-copy input, stored tags, and pipeline settings. |

`application_version_skills.sql` remains the authoritative deletion query.
Its timestamp update uses only the application identity returned by the authorized version deletion.
The sqlc schema projection includes the timestamp column from Main's tenant migration.
Go API bindings, sqlc output, and UI clients are regenerated through their owning generators.

The added regression proves that timestamp failure rolls back version and skill-binding deletion.
The combined skill-copy test also proves that foreign application cleanup changes neither bindings nor timestamps.
The tracing-access fixture now supplies required fields and a real vault sealer without changing its permission expectations.

## Verification

Results below apply to the merged tree, not an earlier deployment.

- Rust formatting and Clippy pass with the locked offline dependency set.
- PostgreSQL-backed Rust tests pass: 905 library tests and 84 integration or contract tests. No tests are ignored.
- Main API and runtime integration selections pass across 63 packages, with 7,014 passing test or subtest events.
- That broad Main run reports 1,263 skipped test events. It does not prove the skipped database or service boundaries.
- Focused PostgreSQL race tests pass for version creation, skill copying, metadata, timestamps, tags, and cleanup.
- The expanded PostgreSQL race selection passes 300 test or subtest cases across four packages, with no skips.
  It covers internal MCP categories, application contracts, tag selection, and concurrent vault writes.
- Go vet passes for the selected Main APIs, repositories, runtime composition, output, storage, and MCP packages.
- Main startup, toolkit naming, and migration-manifest tests pass. Rust documentation builds with warnings denied.
- Gateway `GOWORK=off go test ./internal/llmproxy` passes.
- UI type checking passes. The focused UI run passes 489 tests across 40 files.
- Focused UI lint passes for the merged chat, message, and toolkit editor files.
- sqlc generation and vet pass. The UI clean-generation check matches all 466 generated files.
- `git diff --check` passes.

Database tests create isolated fixtures on the rehearsal service and remove those fixtures afterward.
The sync does not modify existing conversations, credentials, or toolkit definitions.

## Deployment boundary

No service is redeployed during this sync.
Tenant migration `0134_application_updated_at.sql` must run before the merged application repository serves writes.
The database tests apply the migration chain; they do not migrate the existing rehearsal product database.
Rebuild and deploy Main, the web UI, and the gateway before the next browser proof.
Keep browser, real-provider, mixed-guard, replacement, and production proofs open in the [testing register](../testing-gaps.md).

The [remaining-gates register](../remaining-gates.md) defines the next implementation order.
