{
  "spec": "§3.5, last two rows — 300 KB initial, 250 KB per route chunk",
  "measure": "gzip KiB, as the bundler and the browser both report transfer size",
  "note": [
    "scripts/check-bundle-budget.mjs reads this file. The gate fails when the",
    "file is absent, when the build produces a target this file does not",
    "declare, and when this file declares a target the build does not produce.",
    "Raise a number here only with the measurement that justifies it."
  ],
  "targets": {
    "app": {
      "html": "index.html",
      "initialKB": 300,
      "routeChunkKB": 250
    },
    "admin": {
      "html": "index.html",
      "initialNote": [
        "310, not 300: measured 2026-09-07 on feat/platform-parity-wave (PR #823).",
        "main's admin initial set was 295.3 KiB gzip; the wave's 300.3 KiB is that",
        "plus 4.9 KiB of src/shared/i18n/en.json, which grew from 52.8 to 57.7 KiB",
        "gzip with the toolkit catalogue, support assistant, Inventory, Settings",
        "parity and lifecycle strings. The admin entry ships the whole catalogue",
        "(one bundle, no namespace split — see shared/i18n/README.md), so every",
        "app-side string lands in admin's initial set. Splitting the catalogue",
        "per entry is the fix that brings this back to 300; until then the",
        "budget records the measured cost rather than hiding it behind an ",
        "allowlist."
      ],
      "initialKB": 310,
      "routeChunkKB": 250
    },
    "maintenance": {
      "html": "maintenance.html",
      "minJsChunks": 0,
      "initialKB": 300,
      "routeChunkKB": 250
    },
    "brand-preview": {
      "html": "index.html",
      "minJsChunks": 0,
      "initialKB": 300,
      "routeChunkKB": 250
    },
    "docs": {
      "html": "index.html",
      "initialNote": [
        "Own line, not folded into 'app' (PREAMBLE decision 7): a separate Vite",
        "mode with its own dependency set (react + react-dom, minisearch, the",
        "MDX-compiled content/component tree; no MUI). 200 briefly did not",
        "survive the real content merge (embedded-docs W5, 2026-09-08):",
        "`content-registry.ts` imported every `content/**/*.mdx` with",
        "`{ eager: true }`, so all 126 real pages compiled into the entry's one",
        "JS chunk — measured 351.2 KiB gzip.",
        "Fixed by switching `content-registry.ts` to a per-slug LAZY",
        "`import.meta.glob(..., { eager: false })`, each loader wrapped in",
        "React `lazy()` and rendered inside a `<Suspense>` boundary in App.tsx —",
        "only the page a reader is actually on ships its own chunk now.",
        "Frontmatter (title/description, needed synchronously for nav/search/",
        "prev-next/the page's `<h1>`/`document.title`) comes from a small",
        "generated `page-meta.generated.json`, not a second eager MDX glob —",
        "an eager `import: 'frontmatter'` glob of the SAME files made Rollup",
        "fold every page's component back into the eager graph to satisfy the",
        "static import edge (measured 353.2 KiB gzip, no improvement). The",
        "search index (`search-index.generated.json`, ~85 KiB gzip of full",
        "page prose) is also lazy now — `search.ts` dynamic-imports it, and",
        "`minisearch`, only once a reader actually types a query.",
        "Measured 154.2 KiB gzip after the fix (2026-09-08), comfortably under",
        "200.",
        "`mermaid` itself does NOT count here: Mermaid.tsx dynamic-imports it,",
        "so its ~140 KiB gzip parser chunk (plus one further chunk per diagram",
        "type it lazy-loads) lands in routeChunkKB below, not the initial set —",
        "confirmed by index.html carrying no <link rel=modulepreload> for it."
      ],
      "initialKB": 200,
      "routeChunkKB": 250
    }
  }
}
