---
title: "Zephyr Enterprise"
description: "Connect Zephyr Enterprise to Elitea: prerequisites, credential setup and the tools it exposes."
---

Read and manage Zephyr Enterprise test cases and cycles. It's listed under the **test management** category in the toolkit type picker.

## Prerequisites

- An account at Zephyr Enterprise with permission to issue an API token or credential, saved in Elitea as a **`zephyr_enterprise`** credential (`/credentials/create-credential/<type>`).
- If you plan to index content for retrieval: a `pgvector` credential and an embedding model, configured under AI providers.

## Connect the toolkit

<Steps>
<Step title="Open the toolkit type picker">
In the left navigation, open **Toolkits**, then select **New**. Filter the type picker by the **test management** category, or search for "Zephyr Enterprise".
</Step>

<Step title="Pick or create a credential">
Under the credential field, choose an existing **zephyr_enterprise** credential, or select **Create credential** to add one without leaving the form.
</Step>

<Step title="Add indexing settings (optional)">
If you plan to index this source, set an **embedding model** and a **pgvector** credential. Leave both empty to use the toolkit without indexing.
</Step>

<Step title="Save">
Name the toolkit and select **Create**. It's now available to attach to agents, pipelines and conversations in this project.
</Step>
</Steps>

<Screenshot id="toolkit-new-generic" alt="New toolkit dialog with the Zephyr Enterprise type selected" />

## Available tools

<Accordion title="Available tools (11)">

| Tool |
|---|
| Add steps |
| Create testcase |
| Get test case |
| Get testcases by zql |
| Index data |
| List indexes |
| Remove index |
| Search index |
| Search zql |
| Stepback search index |
| Stepback summary index |

</Accordion>

## Common issues

<Note>
On the Rust worker (Helm's default), this tile is hidden — the Rust worker does not materialise `Zephyr Enterprise` (tracked in #865). Use a deployment running the Python worker to connect Zephyr Enterprise.
</Note>

<Note>
The Indexes tab only appears when the configured worker can run this type's `index_data` tool; if the worker can't, the tab is hidden and shows the worker's refusal message. `index_data` itself can't be run from the Test tools pane — use the Indexes tab.
</Note>

<Note>
There's no connection check on this form. Save the toolkit, then run one read-only tool from the editor's Test tools pane to confirm the credential works before attaching Zephyr Enterprise to an agent.
</Note>
