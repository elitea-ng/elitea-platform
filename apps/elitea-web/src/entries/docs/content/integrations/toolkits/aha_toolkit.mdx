---
title: "Aha!"
description: "Connect Aha! to Elitea: prerequisites, credential setup and the tools it exposes."
---

Read and manage Aha! features, ideas and releases. It's listed under the **project management** category in the toolkit type picker.

## Prerequisites

- An account at Aha! with permission to issue an API token or credential, saved in Elitea as a **`aha`** credential (`/credentials/create-credential/<type>`).

## Connect the toolkit

<Steps>
<Step title="Open the toolkit type picker">
In the left navigation, open **Toolkits**, then select **New**. Filter the type picker by the **project management** category, or search for "Aha!".
</Step>

<Step title="Pick or create a credential">
Under the credential field, choose an existing **aha** credential, or select **Create credential** to add one without leaving the form.
</Step>

<Step title="Save">
Name the toolkit and select **Create**. It's now available to attach to agents, pipelines and conversations in this project.
</Step>
</Steps>

<Screenshot id="toolkit-new-generic" alt="New toolkit dialog with the Aha! type selected" />

## Available tools

<Accordion title="Available tools (33)">

| Tool |
|---|
| Add comment |
| Attach file |
| Copy record |
| Create record |
| Create record link |
| Delete record |
| Field options metadata |
| Fields metadata |
| Find project |
| Get epic |
| Get feature |
| Get feature gql |
| Get idea |
| Get initiative |
| Get page |
| Get product |
| Get release |
| Get requirement |
| Get requirement gql |
| List comments |
| List epics |
| List features |
| List ideas |
| List initiatives |
| List products |
| List releases |
| List requirements |
| Manage record |
| Read records |
| Search |
| Search documents |
| Search records |
| Update record |

</Accordion>

## Common issues

<Note>
On the Rust worker, this tile shows greyed out with a reason — the Rust worker does not materialise `Aha!` (tracked in #865). Helm now defaults to the Python worker, matching compose (#865, #866); a deployment that opts into the Rust worker still needs the Python worker to connect Aha!.
</Note>

<Note>
There's no connection check on this form. Save the toolkit, then run one read-only tool from the editor's Test tools pane to confirm the credential works before attaching Aha! to an agent.
</Note>
