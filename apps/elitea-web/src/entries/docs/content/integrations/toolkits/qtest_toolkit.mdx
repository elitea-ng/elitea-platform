---
title: "QTest"
description: "Connect QTest to Elitea: prerequisites, credential setup and the tools it exposes."
---

Read and manage QTest projects, test cases and test runs. It's listed under the **test management** category in the toolkit type picker.

## Prerequisites

- An account at QTest with permission to issue an API token or credential, saved in Elitea as a **`qtest`** credential (`/credentials/create-credential/<type>`).
- If you plan to index content for retrieval: a `pgvector` credential and an embedding model, configured under AI providers.

## Connect the toolkit

<Steps>
<Step title="Open the toolkit type picker">
In the left navigation, open **Toolkits**, then select **New**. Filter the type picker by the **test management** category, or search for "QTest".
</Step>

<Step title="Pick or create a credential">
Under the credential field, choose an existing **qtest** credential, or select **Create credential** to add one without leaving the form.
</Step>

<Step title="Add indexing settings (optional)">
If you plan to index this source, set an **embedding model** and a **pgvector** credential. Leave both empty to use the toolkit without indexing.
</Step>

<Step title="Save">
Name the toolkit and select **Create**. It's now available to attach to agents, pipelines and conversations in this project.
</Step>
</Steps>

<Screenshot id="toolkit-new-generic" alt="New toolkit dialog with the QTest type selected" />

## Available tools

<Accordion title="Available tools (25)">

| Tool |
|---|
| Add file to test case |
| Create test cases |
| Delete test case |
| Find defects by test run id |
| Find entity by id |
| Find requirements by test case id |
| Find test case by id |
| Find test cases by requirement id |
| Find test runs by test case id |
| Get all test cases fields for project |
| Get modules |
| Get test case versions |
| Index data |
| Link tests to jira requirement |
| Link tests to qtest requirement |
| List indexes |
| Remove index |
| Search by dql |
| Search entities by dql |
| Search index |
| Stepback search index |
| Stepback summary index |
| Update test case |
| Update test run status |
| Upload attachment to test run |

</Accordion>

## Common issues

<Note>
On the Rust worker, this tile shows greyed out with a reason — the Rust worker does not materialise `QTest` (tracked in #865). Helm now defaults to the Python worker, matching compose (#865, #866); a deployment that opts into the Rust worker still needs the Python worker to connect QTest.
</Note>

<Note>
The Indexes tab only appears when the configured worker can run this type's `index_data` tool; if the worker can't, the tab is hidden and shows the worker's refusal message. `index_data` itself can't be run from the Test tools pane — use the Indexes tab.
</Note>

<Note>
The credential form for QTest supports **Check connection** — use it before saving to confirm Elitea can reach QTest with these details, instead of discovering an auth problem from a failed tool call later.
</Note>
