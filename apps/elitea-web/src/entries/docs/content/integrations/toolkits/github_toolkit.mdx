---
title: "GitHub"
description: "Connect GitHub to Elitea: prerequisites, credential setup and the tools it exposes."
---

Read and write files, branches, issues and pull requests in a GitHub repository. It's listed under the **code repositories** category in the toolkit type picker.

## Prerequisites

- An account at GitHub with permission to issue an API token or credential, saved in Elitea as a **`github`** credential (`/credentials/create-credential/<type>`).
- If you plan to index content for retrieval: a `pgvector` credential and an embedding model, configured under AI providers.

## Connect the toolkit

<Steps>
<Step title="Open the toolkit type picker">
In the left navigation, open **Toolkits**, then select **New**. Filter the type picker by the **code repositories** category, or search for "GitHub".
</Step>

<Step title="Pick or create a credential">
Under the credential field, choose an existing **github** credential, or select **Create credential** to add one without leaving the form.
</Step>

<Step title="Add indexing settings (optional)">
If you plan to index this source, set an **embedding model** and a **pgvector** credential. Leave both empty to use the toolkit without indexing.
</Step>

<Step title="Save">
Name the toolkit and select **Create**. It's now available to attach to agents, pipelines and conversations in this project.
</Step>
</Steps>

<Screenshot id="toolkit-new-generic" alt="New toolkit dialog with the GitHub type selected" />

## Available tools

<Accordion title="Available tools (44)">

| Tool |
|---|
| Apply git patch |
| Apply git patch from file |
| Comment on issue |
| Create branch |
| Create file |
| Create issue |
| Create issue on project |
| Create pull request |
| Delete branch |
| Delete file |
| Generic github api call |
| Get commit changes |
| Get commits |
| Get commits diff |
| Get files from directory |
| Get issue |
| Get issues |
| Get me |
| Get pull request |
| Get workflow logs |
| Get workflow status |
| Grep file |
| Index data |
| List branches in repo |
| List files in bot branch |
| List files in main branch |
| List indexes |
| List open pull requests |
| List project issues |
| List pull request diffs |
| Read file |
| Read multiple files |
| Remove index |
| Search code |
| Search index |
| Search issues |
| Search project issues |
| Set active branch |
| Stepback search index |
| Stepback summary index |
| Trigger workflow |
| Update file |
| Update issue |
| Update issue on project |

</Accordion>

## Common issues

<Note>
The Indexes tab only appears when the configured worker can run this type's `index_data` tool; if the worker can't, the tab is hidden and shows the worker's refusal message. `index_data` itself can't be run from the Test tools pane — use the Indexes tab.
</Note>

<Note>
There's no connection check on this form. Save the toolkit, then run one read-only tool from the editor's Test tools pane to confirm the credential works before attaching GitHub to an agent.
</Note>
