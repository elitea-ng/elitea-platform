/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Elitea Platform API
 * External REST API for the Elitea Platform (elitea-main Go service).
 * Wire-compatible drop-in replacement for pylon_main (Python/Flask).
 *
 * ## Response shape conventions
 * - **Single resource**: returned directly at top level — `{"id": 42, "name": "..."}`
 * - **General errors (4xx/5xx)**: `{"error": "message"}` (pkg/apierr/apierr.go:47-59
 *   and the handlers' inline `map[string]any{"error": ...}` writes). One
 *   documented shape deviates: the five social/handler.go 500 sites (Like
 *   :268, Unlike :301, Pin :321, Unpin :341, CreateFeedback :419) emit the
 *   pylon-era `{"ok": false, "error": ...}` envelope instead, modeled as
 *   the named `SocialActionErrorResponse` schema and referenced only on
 *   those specific operations. Five more UNDOCUMENTED sites of the same
 *   envelope remain on toolkits/handler.go (:286, 312, 324, 350, 362) —
 *   scope that shape there when those domains are spec'd.
 * - **Rate limit (429)**: intentionally NOT documented. The Go RateLimit
 *   middleware is a pass-through stub (internal/api/middleware/ratelimit.go:9-14)
 *   and no elitea-main code emits a 429 body — documenting one would be contract
 *   fiction. Re-add per-operation 429 responses (pylon shape:
 *   `{"error": {"message", "type": "rate_limit_error", "code":
 *   "rate_limit_exceeded"}}`) when rate limiting becomes real.
 * - Field names are **snake_case** unless the Go handler observably marshals
 *   otherwise (e.g. `lastModified` in the S3 listing, `DisplayName`/`ID` in the
 *   S3 owner block).
 *
 * ## NOTE(W2) — field-level enrichment conventions
 * Every schema below is derived from the Go handler that actually marshals the
 * response (UI reimplementation spec §5.1 / §9.3 unit W2). Evidence is recorded
 * per schema in `NOTE(W2)` comments/descriptions with file:line references.
 * Where a handler genuinely passes through arbitrary JSON (DB `jsonb` columns,
 * echoed request bodies), the schema carries an
 * `x-elitea-passthrough: "NOTE(W2): ..."` marker justifying the permissive
 * `additionalProperties`. A permissive object WITHOUT that marker is a defect —
 * `apps/elitea-web/scripts/check-contract-coverage.mjs` fails it, wired as the
 * "Contract coverage (§5.1)" step of ci-web.yml's `gates` job.
 *
 * OpenAPI spec version: 2.0.0
 */
import { useQuery } from "@tanstack/react-query";
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseQueryOptions,
  UseQueryResult,
} from "@tanstack/react-query";

import type {
  GetSearchOptionsParams,
  N400Response,
  N401Response,
  N403Response,
  SearchOptions,
} from "../model";

import { eliteaFetch } from ".././mutator";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

const withQueryKey = <T extends object, K>(
  query: T,
  queryKey: K,
): T & { queryKey: K } => {
  const result = { queryKey } as T & { queryKey: K };
  for (const key of Object.keys(query)) {
    // The explicit queryKey always wins, matching the previous
    // `{ ...query, queryKey }` spread where it was set last.
    if (key === "queryKey") continue;
    Object.defineProperty(result, key, {
      enumerable: true,
      configurable: true,
      get: () => (query as Record<string, unknown>)[key],
    });
  }
  return result;
};

export type getSearchOptionsResponse200 = {
  data: SearchOptions;
  status: 200;
};

export type getSearchOptionsResponse400 = {
  data: N400Response;
  status: 400;
};

export type getSearchOptionsResponse401 = {
  data: N401Response;
  status: 401;
};

export type getSearchOptionsResponse403 = {
  data: N403Response;
  status: 403;
};

export type getSearchOptionsResponseSuccess = getSearchOptionsResponse200 & {
  headers: Headers;
};
export type getSearchOptionsResponseError = (
  | getSearchOptionsResponse400
  | getSearchOptionsResponse401
  | getSearchOptionsResponse403
) & {
  headers: Headers;
};

export type getSearchOptionsResponse =
  getSearchOptionsResponseSuccess | getSearchOptionsResponseError;

export const getGetSearchOptionsUrl = (
  projectId: string,
  params: GetSearchOptionsParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["entities[]", "tags[]", "statuses[]"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/elitea_core/search_options/prompt_lib/${projectId}?${stringifiedParams}`
    : `/elitea_core/search_options/prompt_lib/${projectId}`;
};

/**
 * @summary Get visible entity search options
 */
export const getSearchOptions = async (
  projectId: string,
  params: GetSearchOptionsParams,
  options?: Parameters<typeof eliteaFetch>[1],
): Promise<getSearchOptionsResponse> => {
  return eliteaFetch<getSearchOptionsResponse>(
    getGetSearchOptionsUrl(projectId, params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getGetSearchOptionsQueryKey = (
  projectId: string,
  params?: GetSearchOptionsParams,
) => {
  return [
    `/elitea_core/search_options/prompt_lib/${projectId}`,
    ...(params ? [params] : []),
  ] as const;
};

export const getGetSearchOptionsQueryOptions = <
  TData = Awaited<ReturnType<typeof getSearchOptions>>,
  TError = N400Response | N401Response | N403Response,
>(
  projectId: string,
  params: GetSearchOptionsParams,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof getSearchOptions>>,
        TError,
        TData
      >
    >;
    request?: SecondParameter<typeof eliteaFetch>;
  },
) => {
  const { query: queryOptions, request: requestOptions } = options ?? {};

  const queryKey =
    queryOptions?.queryKey ?? getGetSearchOptionsQueryKey(projectId, params);

  const queryFn: QueryFunction<
    Awaited<ReturnType<typeof getSearchOptions>>
  > = ({ signal }) =>
    getSearchOptions(projectId, params, { signal, ...requestOptions });

  return {
    queryKey,
    queryFn,
    enabled: projectId !== null && projectId !== undefined,
    ...queryOptions,
  } as UseQueryOptions<
    Awaited<ReturnType<typeof getSearchOptions>>,
    TError,
    TData
  > & { queryKey: DataTag<QueryKey, TData, TError> };
};

export type GetSearchOptionsQueryResult = NonNullable<
  Awaited<ReturnType<typeof getSearchOptions>>
>;
export type GetSearchOptionsQueryError =
  N400Response | N401Response | N403Response;

export function useGetSearchOptions<
  TData = Awaited<ReturnType<typeof getSearchOptions>>,
  TError = N400Response | N401Response | N403Response,
>(
  projectId: string,
  params: GetSearchOptionsParams,
  options: {
    query: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof getSearchOptions>>,
        TError,
        TData
      >
    > &
      Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof getSearchOptions>>,
          TError,
          Awaited<ReturnType<typeof getSearchOptions>>
        >,
        "initialData"
      >;
    request?: SecondParameter<typeof eliteaFetch>;
  },
  queryClient?: QueryClient,
): DefinedUseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>;
};
export function useGetSearchOptions<
  TData = Awaited<ReturnType<typeof getSearchOptions>>,
  TError = N400Response | N401Response | N403Response,
>(
  projectId: string,
  params: GetSearchOptionsParams,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof getSearchOptions>>,
        TError,
        TData
      >
    > &
      Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof getSearchOptions>>,
          TError,
          Awaited<ReturnType<typeof getSearchOptions>>
        >,
        "initialData"
      >;
    request?: SecondParameter<typeof eliteaFetch>;
  },
  queryClient?: QueryClient,
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>;
};
export function useGetSearchOptions<
  TData = Awaited<ReturnType<typeof getSearchOptions>>,
  TError = N400Response | N401Response | N403Response,
>(
  projectId: string,
  params: GetSearchOptionsParams,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof getSearchOptions>>,
        TError,
        TData
      >
    >;
    request?: SecondParameter<typeof eliteaFetch>;
  },
  queryClient?: QueryClient,
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>;
};
/**
 * @summary Get visible entity search options
 */

export function useGetSearchOptions<
  TData = Awaited<ReturnType<typeof getSearchOptions>>,
  TError = N400Response | N401Response | N403Response,
>(
  projectId: string,
  params: GetSearchOptionsParams,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof getSearchOptions>>,
        TError,
        TData
      >
    >;
    request?: SecondParameter<typeof eliteaFetch>;
  },
  queryClient?: QueryClient,
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>;
} {
  const queryOptions = getGetSearchOptionsQueryOptions(
    projectId,
    params,
    options,
  );

  const query = useQuery(queryOptions, queryClient) as UseQueryResult<
    TData,
    TError
  > & { queryKey: DataTag<QueryKey, TData, TError> };

  return withQueryKey(query, queryOptions.queryKey);
}
