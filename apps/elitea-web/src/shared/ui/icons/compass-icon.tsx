/**
 * Ported from `EliteaUI/src/assets/compass.svg`.
 *
 * The reference drawer maps `project-context` to this glyph and
 * `project-general` to the briefcase (`SettingsDrawer.jsx:30-31`). This app
 * had only the briefcase, so Project Context borrowed it — and once the
 * General tab landed, two adjacent nav rows would have drawn the same icon.
 */
export { default as CompassIcon } from './svg/compass-icon.svg?react';
