/**
 * Half-filled circle — the "System" option of the theme toggle.
 *
 * The production UI renders MUI's own `Contrast` glyph here; this is that
 * path, kept local so the toggle's three icons come from one place.
 *
 * @public Icon surface: consumed by `shared/ui/ThemeModeToggle`.
 */
export { default as ContrastIcon } from './svg/contrast-icon.svg?react';
